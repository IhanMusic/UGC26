# UGC26 — Complete i18n (EN / FR / AR) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wire all new MVP components to the next-intl translation system — add missing keys to all three locale files (EN/FR/AR) and replace every hardcoded string in the new components with `t("key")` calls.

**Architecture:** next-intl is already installed and configured. Server components use `getTranslations("namespace")` from `next-intl/server`; client components use `useTranslations("namespace")` from `next-intl`. All string literals in new MVP components are replaced with translation keys organised in new namespaces added to `messages/en.json`, `messages/fr.json`, `messages/ar.json`.

**Tech Stack:** next-intl, TypeScript, Next.js 16 App Router (server + client components)

---

## File Map

### Modified: translation files
- `messages/en.json` — add new namespaces
- `messages/fr.json` — add new namespaces (French)
- `messages/ar.json` — add new namespaces (Arabic, RTL)

### Modified: components
- `src/components/verification-banner.tsx` — use `useTranslations("emailVerification")`
- `src/components/file-upload.tsx` — accept translated `label` prop; remove hardcoded upload string
- `src/components/deliverable-reject-modal.tsx` — use `useTranslations("deliverables")`
- `src/components/company-deliverables-tab.tsx` — use `useTranslations("deliverables")`
- `src/components/influencer-deliverables-tab.tsx` — use `useTranslations("deliverables")`
- `src/components/review-modal.tsx` — use `useTranslations("reviews")`
- `src/components/review-banner.tsx` — use `useTranslations("reviews")`
- `src/components/dispute-modal.tsx` — use `useTranslations("disputes")`
- `src/components/campaign-chat.tsx` — use `useTranslations("messaging")`

### Modified: pages
- `src/app/[locale]/auth/verify-email/page.tsx` — use `getTranslations("emailVerification")`
- `src/app/[locale]/company/campaigns/[id]/client.tsx` — use `useTranslations("campaignDetail")` + `useTranslations("deliverables")`
- `src/app/[locale]/influencer/campaigns/[id]/client.tsx` — use `useTranslations("campaignDetail")`
- `src/app/[locale]/influencer/payments/page.tsx` — use `getTranslations("payments")`
- `src/app/[locale]/influencer/payments/bank-details-form.tsx` — use `useTranslations("payments")`
- `src/app/[locale]/admin/influencers/page.tsx` — use `getTranslations("admin")`
- `src/app/[locale]/admin/companies/page.tsx` — use `getTranslations("admin")`
- `src/app/[locale]/admin/campaigns/page.tsx` — use `getTranslations("admin")`

---

## Task 1: Add all new translation keys to all 3 locale files

**Files:**
- Modify: `messages/en.json`
- Modify: `messages/fr.json`
- Modify: `messages/ar.json`

- [ ] **Step 1: Add new keys to `messages/en.json`**

Append these new sections inside the root JSON object (before the closing `}`):

```json
  "emailVerification": {
    "bannerText": "📧 Verify your email to access all features.",
    "resend": "Resend email",
    "sending": "Sending...",
    "sent": "Email sent ✓",
    "error": "Error sending email",
    "verifyTitle": "Check your email",
    "verifyDesc": "A verification email has been sent to your address. Click the link in the email to activate your account.",
    "invalidTitle": "Invalid or expired link",
    "invalidDesc": "This verification link is no longer valid. Log in and click 'Resend email'.",
    "backToLogin": "Back to login"
  },
  "fileUpload": {
    "label": "Choose an image",
    "uploading": "Uploading...",
    "error": "Upload failed"
  },
  "deliverables": {
    "title": "Deliverables",
    "newDeliverable": "New deliverable",
    "typeLabel": "Type",
    "briefLabel": "Brief / Description",
    "briefPlaceholder": "Describe the expected content...",
    "assignToLabel": "Assign to",
    "allInfluencers": "All influencers",
    "createButton": "Create deliverable",
    "creating": "Creating...",
    "createError": "Error creating deliverable",
    "noneCreated": "No deliverables created.",
    "noneAssigned": "No deliverables assigned yet.",
    "loading": "Loading...",
    "approve": "✓ Approve",
    "approveError": "Error approving deliverable",
    "reject": "✗ Reject",
    "rejectTitle": "Reject deliverable",
    "reasonLabel": "Reason",
    "debriefLabel": "Debrief message",
    "debriefRequired": "*",
    "debriefPlaceholder": "Explain the required changes...",
    "debriefMissing": "Debrief message is required",
    "confirmReject": "Confirm rejection",
    "rejecting": "Sending...",
    "rejectError": "Error rejecting",
    "feedbackLabel": "Company feedback:",
    "publicationLinkLabel": "Publication link",
    "newPublicationLinkLabel": "New publication link",
    "linkPlaceholder": "https://www.instagram.com/p/...",
    "submitButton": "Submit",
    "submitting": "...",
    "submitError": "Submission error",
    "viewSubmission": "View my submission →",
    "viewPublicationLink": "View publication link →",
    "progressLabel": "Progress",
    "approvedCount": "{approved}/{total} approved",
    "statusPending": "Pending",
    "statusSubmitted": "Waiting for validation",
    "statusApproved": "Approved ✓",
    "statusRejected": "Rejected",
    "reasons": {
      "badQuality": "Poor visual quality",
      "doesntMatchBrief": "Does not match brief",
      "invalidLink": "Invalid link",
      "other": "Other"
    },
    "types": {
      "INSTAGRAM_POST": "Instagram Post",
      "STORY": "Story",
      "REEL": "Reel",
      "VIDEO": "Video",
      "TIKTOK": "TikTok",
      "YOUTUBE": "YouTube",
      "OTHER": "Other"
    }
  },
  "campaignDetail": {
    "tabInfos": "Info",
    "tabApplicants": "Applicants",
    "tabDeliverables": "Deliverables",
    "tabMessages": "Messages",
    "tabDetails": "Details",
    "tabExecution": "Execution",
    "noApplicants": "No applicants yet.",
    "viewLink": "View →",
    "statusLabel": "Status",
    "budgetLabel": "Budget",
    "platformsLabel": "Platforms",
    "countryLabel": "Country",
    "minFollowersLabel": "Min followers",
    "ageRangeLabel": "Age range",
    "createdAtLabel": "Created",
    "scheduledAtLabel": "Scheduled start",
    "descriptionLabel": "Description",
    "openDispute": "Open a dispute",
    "disputeSubmitted": "Dispute submitted. The team will contact you shortly.",
    "messagesComingSoon": "Messages — coming soon",
    "noConversation": "No conversation for this campaign.",
    "participation": {
      "UPCOMING": "Upcoming",
      "ONGOING": "Ongoing",
      "COMPLETED": "Completed",
      "CONFIRMED": "Confirmed",
      "PAID": "Paid"
    }
  },
  "payments": {
    "title": "Payments",
    "subtitle": "Your earnings and bank details",
    "totalEarned": "Total earned",
    "pendingAmount": "Pending",
    "historyTitle": "History",
    "noTransactions": "No transactions.",
    "colDate": "Date",
    "colAmount": "Amount",
    "colStatus": "Status",
    "bankDetailsTitle": "Bank details",
    "bankDetailsDesc": "This information is used for your payments.",
    "bankNameLabel": "Bank name",
    "accountHolderLabel": "Account holder",
    "ibanLabel": "IBAN / RIB",
    "bankNamePlaceholder": "CPA, BNA, BEA...",
    "accountHolderPlaceholder": "First Last",
    "ibanPlaceholder": "DZ...",
    "saveButton": "Save",
    "saving": "Saving...",
    "savedConfirm": "Details saved ✓",
    "saveError": "Error saving",
    "loading": "Loading..."
  }
```

Also extend the existing `"reviews"` section with:
```json
    "campaignDone": "⭐ Campaign completed!",
    "leaveReviewFor": "Leave a review for {name}",
    "leaveReviewButton": "Leave a review →",
    "yourExperience": "Your experience with {name}",
    "noteLabel": "Rating",
    "required": "*",
    "commentLabel": "Comment",
    "optional": "(optional)",
    "commentPlaceholder": "Share your experience...",
    "publishButton": "Publish review ✓",
    "publishing": "Publishing...",
    "selectRating": "Please select a rating",
    "submitError": "Error"
```

Also extend the existing `"disputes"` section with:
```json
    "modalTitle": "Open a dispute",
    "modalDesc": "Describe the problem. The UGC26 team will intervene within 48h.",
    "reasonLabel": "Dispute reason",
    "required": "*",
    "reasonPlaceholder": "Explain your problem in detail...",
    "minLength": "Please describe your problem in at least 10 characters",
    "submitting": "Sending...",
    "openButton": "Open a dispute",
    "successMessage": "Dispute submitted. The team will contact you shortly."
```

Also extend the existing `"messaging"` section with:
```json
    "noConversation": "No conversation for this campaign.",
    "noMessages": "No messages yet. Start the conversation!",
    "sendButton": "→"
```

Also extend the existing `"admin"` section with:
```json
    "disputes": "Disputes",
    "searchInfluencer": "Search an influencer...",
    "searchCompany": "Search a company...",
    "searchCampaign": "Search a campaign...",
    "filterByStatus": "Filter by status",
    "allStatuses": "All statuses",
    "noResultsFor": "No results for \"{query}\".",
    "noResultsEmpty": "No results.",
    "pageLabel": "Page",
    "previousPage": "← Previous",
    "nextPage": "Next →",
    "totalCount": "{count} total"
```

- [ ] **Step 2: Add new keys to `messages/fr.json`**

Add the same structure with French translations:

```json
  "emailVerification": {
    "bannerText": "📧 Vérifiez votre email pour accéder à toutes les fonctionnalités.",
    "resend": "Renvoyer l'email",
    "sending": "Envoi...",
    "sent": "Email envoyé ✓",
    "error": "Erreur lors de l'envoi",
    "verifyTitle": "Vérifiez votre email",
    "verifyDesc": "Un email de vérification a été envoyé à votre adresse. Cliquez sur le lien dans l'email pour activer votre compte.",
    "invalidTitle": "Lien invalide ou expiré",
    "invalidDesc": "Ce lien de vérification n'est plus valide. Connectez-vous et cliquez sur 'Renvoyer l'email'.",
    "backToLogin": "Retour à la connexion"
  },
  "fileUpload": {
    "label": "Choisir une image",
    "uploading": "Upload en cours...",
    "error": "Échec de l'upload"
  },
  "deliverables": {
    "title": "Livrables",
    "newDeliverable": "Nouveau livrable",
    "typeLabel": "Type",
    "briefLabel": "Brief / Description",
    "briefPlaceholder": "Décrivez le contenu attendu...",
    "assignToLabel": "Assigner à",
    "allInfluencers": "Tous les influenceurs",
    "createButton": "Créer le livrable",
    "creating": "Création...",
    "createError": "Erreur lors de la création",
    "noneCreated": "Aucun livrable créé.",
    "noneAssigned": "Aucun livrable assigné pour le moment.",
    "loading": "Chargement...",
    "approve": "✓ Approuver",
    "approveError": "Erreur lors de l'approbation",
    "reject": "✗ Rejeter",
    "rejectTitle": "Rejeter le livrable",
    "reasonLabel": "Raison",
    "debriefLabel": "Message de débrief",
    "debriefRequired": "*",
    "debriefPlaceholder": "Expliquez les modifications nécessaires...",
    "debriefMissing": "Le message de débrief est obligatoire",
    "confirmReject": "Confirmer le rejet",
    "rejecting": "Envoi...",
    "rejectError": "Erreur lors du rejet",
    "feedbackLabel": "Feedback de l'entreprise :",
    "publicationLinkLabel": "Lien de publication",
    "newPublicationLinkLabel": "Nouveau lien de publication",
    "linkPlaceholder": "https://www.instagram.com/p/...",
    "submitButton": "Soumettre",
    "submitting": "...",
    "submitError": "Erreur lors de la soumission",
    "viewSubmission": "Voir ma soumission →",
    "viewPublicationLink": "Voir le lien de publication →",
    "progressLabel": "Progression",
    "approvedCount": "{approved}/{total} approuvés",
    "statusPending": "En attente",
    "statusSubmitted": "En cours de validation",
    "statusApproved": "Approuvé ✓",
    "statusRejected": "Rejeté",
    "reasons": {
      "badQuality": "Mauvaise qualité visuelle",
      "doesntMatchBrief": "Ne respecte pas le brief",
      "invalidLink": "Lien invalide",
      "other": "Autre"
    },
    "types": {
      "INSTAGRAM_POST": "Instagram Post",
      "STORY": "Story",
      "REEL": "Reel",
      "VIDEO": "Vidéo",
      "TIKTOK": "TikTok",
      "YOUTUBE": "YouTube",
      "OTHER": "Autre"
    }
  },
  "campaignDetail": {
    "tabInfos": "Infos",
    "tabApplicants": "Candidats",
    "tabDeliverables": "Livrables",
    "tabMessages": "Messages",
    "tabDetails": "Détails",
    "tabExecution": "Exécution",
    "noApplicants": "Aucun candidat pour l'instant.",
    "viewLink": "Voir →",
    "statusLabel": "Statut",
    "budgetLabel": "Budget",
    "platformsLabel": "Plateformes",
    "countryLabel": "Pays",
    "minFollowersLabel": "Min abonnés",
    "ageRangeLabel": "Tranche d'âge",
    "createdAtLabel": "Créé le",
    "scheduledAtLabel": "Début prévu",
    "descriptionLabel": "Description",
    "openDispute": "Ouvrir un dispute",
    "disputeSubmitted": "Dispute soumis. L'équipe vous contactera bientôt.",
    "messagesComingSoon": "Messages — à venir",
    "noConversation": "Aucune conversation pour cette campagne.",
    "participation": {
      "UPCOMING": "À venir",
      "ONGOING": "En cours",
      "COMPLETED": "Complété",
      "CONFIRMED": "Confirmé",
      "PAID": "Payé"
    }
  },
  "payments": {
    "title": "Paiements",
    "subtitle": "Vos revenus et coordonnées bancaires",
    "totalEarned": "Total gagné",
    "pendingAmount": "En attente",
    "historyTitle": "Historique",
    "noTransactions": "Aucune transaction.",
    "colDate": "Date",
    "colAmount": "Montant",
    "colStatus": "Statut",
    "bankDetailsTitle": "Coordonnées bancaires",
    "bankDetailsDesc": "Ces informations sont utilisées pour vos paiements.",
    "bankNameLabel": "Nom de la banque",
    "accountHolderLabel": "Titulaire du compte",
    "ibanLabel": "IBAN / RIB",
    "bankNamePlaceholder": "CPA, BNA, BEA...",
    "accountHolderPlaceholder": "Prénom Nom",
    "ibanPlaceholder": "DZ...",
    "saveButton": "Sauvegarder",
    "saving": "Sauvegarde...",
    "savedConfirm": "Coordonnées sauvegardées ✓",
    "saveError": "Erreur lors de la sauvegarde",
    "loading": "Chargement..."
  }
```

Also extend `"reviews"` in fr.json with:
```json
    "campaignDone": "⭐ Campagne terminée !",
    "leaveReviewFor": "Laissez un avis pour {name}",
    "leaveReviewButton": "Laisser un avis →",
    "yourExperience": "Votre expérience avec {name}",
    "noteLabel": "Note",
    "required": "*",
    "commentLabel": "Commentaire",
    "optional": "(optionnel)",
    "commentPlaceholder": "Partagez votre expérience...",
    "publishButton": "Publier l'avis ✓",
    "publishing": "Publication...",
    "selectRating": "Veuillez sélectionner une note",
    "submitError": "Erreur"
```

Also extend `"disputes"` in fr.json with:
```json
    "modalTitle": "Ouvrir un dispute",
    "modalDesc": "Décrivez le problème rencontré. L'équipe UGC26 interviendra sous 48h.",
    "reasonLabel": "Raison du dispute",
    "required": "*",
    "reasonPlaceholder": "Expliquez votre problème en détail...",
    "minLength": "Veuillez décrire votre problème en au moins 10 caractères",
    "submitting": "Envoi...",
    "openButton": "Ouvrir un dispute",
    "successMessage": "Dispute soumis. L'équipe vous contactera bientôt."
```

Also extend `"messaging"` in fr.json with:
```json
    "noConversation": "Aucune conversation pour cette campagne.",
    "noMessages": "Aucun message pour le moment. Démarrez la conversation !",
    "sendButton": "→"
```

Also extend `"admin"` in fr.json with:
```json
    "disputes": "Disputes",
    "searchInfluencer": "Rechercher un influenceur...",
    "searchCompany": "Rechercher une entreprise...",
    "searchCampaign": "Rechercher une campagne...",
    "filterByStatus": "Filtrer par statut",
    "allStatuses": "Tous les statuts",
    "noResultsFor": "Aucun résultat pour \"{query}\".",
    "noResultsEmpty": "Aucun résultat.",
    "pageLabel": "Page",
    "previousPage": "← Précédent",
    "nextPage": "Suivant →",
    "totalCount": "{count} au total"
```

- [ ] **Step 3: Add new keys to `messages/ar.json`**

Add the same structure with Arabic translations:

```json
  "emailVerification": {
    "bannerText": "📧 تحقق من بريدك الإلكتروني للوصول إلى جميع الميزات.",
    "resend": "إعادة إرسال البريد",
    "sending": "جاري الإرسال...",
    "sent": "تم إرسال البريد ✓",
    "error": "خطأ في الإرسال",
    "verifyTitle": "تحقق من بريدك الإلكتروني",
    "verifyDesc": "تم إرسال بريد إلكتروني للتحقق إلى عنوانك. انقر على الرابط في البريد لتفعيل حسابك.",
    "invalidTitle": "رابط غير صالح أو منتهي الصلاحية",
    "invalidDesc": "هذا الرابط لم يعد صالحاً. سجل الدخول وانقر على 'إعادة إرسال البريد'.",
    "backToLogin": "العودة إلى تسجيل الدخول"
  },
  "fileUpload": {
    "label": "اختر صورة",
    "uploading": "جاري الرفع...",
    "error": "فشل الرفع"
  },
  "deliverables": {
    "title": "المسلّمات",
    "newDeliverable": "مسلّم جديد",
    "typeLabel": "النوع",
    "briefLabel": "الموجز / الوصف",
    "briefPlaceholder": "صف المحتوى المطلوب...",
    "assignToLabel": "تعيين إلى",
    "allInfluencers": "جميع المؤثرين",
    "createButton": "إنشاء المسلّم",
    "creating": "جاري الإنشاء...",
    "createError": "خطأ في الإنشاء",
    "noneCreated": "لم يتم إنشاء أي مسلّم.",
    "noneAssigned": "لا توجد مسلّمات مخصصة بعد.",
    "loading": "جاري التحميل...",
    "approve": "✓ موافقة",
    "approveError": "خطأ في الموافقة",
    "reject": "✗ رفض",
    "rejectTitle": "رفض المسلّم",
    "reasonLabel": "السبب",
    "debriefLabel": "رسالة التقييم",
    "debriefRequired": "*",
    "debriefPlaceholder": "اشرح التعديلات المطلوبة...",
    "debriefMissing": "رسالة التقييم مطلوبة",
    "confirmReject": "تأكيد الرفض",
    "rejecting": "جاري الإرسال...",
    "rejectError": "خطأ في الرفض",
    "feedbackLabel": "ملاحظات الشركة:",
    "publicationLinkLabel": "رابط النشر",
    "newPublicationLinkLabel": "رابط نشر جديد",
    "linkPlaceholder": "https://www.instagram.com/p/...",
    "submitButton": "إرسال",
    "submitting": "...",
    "submitError": "خطأ في الإرسال",
    "viewSubmission": "عرض تسليمي ←",
    "viewPublicationLink": "عرض رابط النشر ←",
    "progressLabel": "التقدم",
    "approvedCount": "{approved}/{total} تمت الموافقة عليهم",
    "statusPending": "في الانتظار",
    "statusSubmitted": "في انتظار المراجعة",
    "statusApproved": "تمت الموافقة ✓",
    "statusRejected": "مرفوض",
    "reasons": {
      "badQuality": "جودة بصرية سيئة",
      "doesntMatchBrief": "لا يتوافق مع الموجز",
      "invalidLink": "رابط غير صالح",
      "other": "أخرى"
    },
    "types": {
      "INSTAGRAM_POST": "منشور إنستغرام",
      "STORY": "قصة",
      "REEL": "ريل",
      "VIDEO": "فيديو",
      "TIKTOK": "تيك توك",
      "YOUTUBE": "يوتيوب",
      "OTHER": "أخرى"
    }
  },
  "campaignDetail": {
    "tabInfos": "المعلومات",
    "tabApplicants": "المتقدمون",
    "tabDeliverables": "المسلّمات",
    "tabMessages": "الرسائل",
    "tabDetails": "التفاصيل",
    "tabExecution": "التنفيذ",
    "noApplicants": "لا يوجد متقدمون حتى الآن.",
    "viewLink": "عرض ←",
    "statusLabel": "الحالة",
    "budgetLabel": "الميزانية",
    "platformsLabel": "المنصات",
    "countryLabel": "البلد",
    "minFollowersLabel": "الحد الأدنى للمتابعين",
    "ageRangeLabel": "الفئة العمرية",
    "createdAtLabel": "تاريخ الإنشاء",
    "scheduledAtLabel": "تاريخ البدء المجدول",
    "descriptionLabel": "الوصف",
    "openDispute": "فتح نزاع",
    "disputeSubmitted": "تم تقديم النزاع. سيتصل بك الفريق قريباً.",
    "messagesComingSoon": "الرسائل — قريباً",
    "noConversation": "لا توجد محادثة لهذه الحملة.",
    "participation": {
      "UPCOMING": "قادمة",
      "ONGOING": "جارية",
      "COMPLETED": "مكتملة",
      "CONFIRMED": "مؤكدة",
      "PAID": "مدفوعة"
    }
  },
  "payments": {
    "title": "المدفوعات",
    "subtitle": "أرباحك والبيانات البنكية",
    "totalEarned": "إجمالي الأرباح",
    "pendingAmount": "في الانتظار",
    "historyTitle": "السجل",
    "noTransactions": "لا توجد معاملات.",
    "colDate": "التاريخ",
    "colAmount": "المبلغ",
    "colStatus": "الحالة",
    "bankDetailsTitle": "البيانات البنكية",
    "bankDetailsDesc": "تُستخدم هذه المعلومات لمعالجة مدفوعاتك.",
    "bankNameLabel": "اسم البنك",
    "accountHolderLabel": "صاحب الحساب",
    "ibanLabel": "IBAN / رقم الحساب",
    "bankNamePlaceholder": "CPA, BNA, BEA...",
    "accountHolderPlaceholder": "الاسم الأول الاسم الأخير",
    "ibanPlaceholder": "DZ...",
    "saveButton": "حفظ",
    "saving": "جاري الحفظ...",
    "savedConfirm": "تم حفظ البيانات ✓",
    "saveError": "خطأ في الحفظ",
    "loading": "جاري التحميل..."
  }
```

Also extend `"reviews"` in ar.json with:
```json
    "campaignDone": "⭐ انتهت الحملة!",
    "leaveReviewFor": "اترك تقييماً لـ {name}",
    "leaveReviewButton": "اترك تقييماً ←",
    "yourExperience": "تجربتك مع {name}",
    "noteLabel": "التقييم",
    "required": "*",
    "commentLabel": "تعليق",
    "optional": "(اختياري)",
    "commentPlaceholder": "شاركنا تجربتك...",
    "publishButton": "نشر التقييم ✓",
    "publishing": "جاري النشر...",
    "selectRating": "الرجاء اختيار تقييم",
    "submitError": "خطأ"
```

Also extend `"disputes"` in ar.json with:
```json
    "modalTitle": "فتح نزاع",
    "modalDesc": "صف المشكلة التي واجهتها. سيتدخل فريق UGC26 خلال 48 ساعة.",
    "reasonLabel": "سبب النزاع",
    "required": "*",
    "reasonPlaceholder": "اشرح مشكلتك بالتفصيل...",
    "minLength": "الرجاء وصف مشكلتك بـ 10 أحرف على الأقل",
    "submitting": "جاري الإرسال...",
    "openButton": "فتح نزاع",
    "successMessage": "تم تقديم النزاع. سيتصل بك الفريق قريباً."
```

Also extend `"messaging"` in ar.json with:
```json
    "noConversation": "لا توجد محادثة لهذه الحملة.",
    "noMessages": "لا توجد رسائل بعد. ابدأ المحادثة!",
    "sendButton": "←"
```

Also extend `"admin"` in ar.json with:
```json
    "disputes": "النزاعات",
    "searchInfluencer": "البحث عن مؤثر...",
    "searchCompany": "البحث عن شركة...",
    "searchCampaign": "البحث عن حملة...",
    "filterByStatus": "تصفية حسب الحالة",
    "allStatuses": "جميع الحالات",
    "noResultsFor": "لا توجد نتائج لـ \"{query}\".",
    "noResultsEmpty": "لا توجد نتائج.",
    "pageLabel": "صفحة",
    "previousPage": "→ السابق",
    "nextPage": "التالي ←",
    "totalCount": "{count} إجمالاً"
```

- [ ] **Step 4: Commit**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26
git add messages/en.json messages/fr.json messages/ar.json
git commit -m "feat(i18n): add all new MVP translation keys (EN/FR/AR)"
```

---

## Task 2: Wire translations into VerificationBanner and verify-email page

**Files:**
- Modify: `src/components/verification-banner.tsx`
- Modify: `src/app/[locale]/auth/verify-email/page.tsx`

- [ ] **Step 1: Update `src/components/verification-banner.tsx`**

Replace the current file with:

```typescript
"use client";

import { useState } from "react";
import { useSession } from "next-auth/react";
import { useTranslations } from "next-intl";

export function VerificationBanner() {
  const { data: session } = useSession();
  const t = useTranslations("emailVerification");
  const [resending, setResending] = useState(false);
  const [sent, setSent] = useState(false);
  const [error, setError] = useState<string | null>(null);

  if (!session?.user || session.user.isVerified) return null;

  const handleResend = async () => {
    setResending(true);
    setError(null);
    try {
      const res = await fetch("/api/auth/resend-verification", { method: "POST" });
      const data = await res.json();
      if (!res.ok) throw new Error(data.error || t("error"));
      setSent(true);
    } catch (e) {
      setError(e instanceof Error ? e.message : t("error"));
    } finally {
      setResending(false);
    }
  };

  return (
    <div className="flex items-center justify-between gap-3 border-b border-amber-500/20 bg-amber-500/10 px-4 py-2.5 text-sm">
      <span className="text-amber-200">{t("bannerText")}</span>
      {sent ? (
        <span className="text-emerald-400 text-xs">{t("sent")}</span>
      ) : (
        <button
          onClick={handleResend}
          disabled={resending}
          className="shrink-0 rounded-lg border border-amber-500/30 bg-amber-500/20 px-3 py-1 text-xs font-medium text-amber-200 transition-colors hover:bg-amber-500/30 disabled:opacity-50"
        >
          {resending ? t("sending") : t("resend")}
        </button>
      )}
      {error && <span className="text-xs text-[#F43F5E]">{error}</span>}
    </div>
  );
}
```

- [ ] **Step 2: Update `src/app/[locale]/auth/verify-email/page.tsx`**

Replace the current file with:

```typescript
import { getTranslations } from "next-intl/server";
import { Link } from "@/i18n/navigation";

interface Props {
  searchParams: Promise<{ error?: string }>;
}

export default async function VerifyEmailPage({ searchParams }: Props) {
  const params = await searchParams;
  const t = await getTranslations("emailVerification");
  const hasError = !!params.error;

  return (
    <div className="flex min-h-screen items-center justify-center bg-[#080B18] px-4">
      <div className="glass w-full max-w-md rounded-2xl p-8 text-center">
        <div className="mb-6 text-5xl">{hasError ? "❌" : "📧"}</div>
        <h1 className="mb-3 text-2xl font-bold text-[#E2E8F0]">
          {hasError ? t("invalidTitle") : t("verifyTitle")}
        </h1>
        <p className="text-[#94A3B8]">
          {hasError ? t("invalidDesc") : t("verifyDesc")}
        </p>
        <Link
          href="/auth/login"
          className="mt-6 inline-block rounded-xl bg-gradient-to-r from-violet-600 to-indigo-600 px-6 py-2.5 text-sm font-semibold text-white transition-opacity hover:opacity-90"
        >
          {t("backToLogin")}
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/verification-banner.tsx "src/app/[locale]/auth/verify-email/page.tsx"
git commit -m "feat(i18n): wire translations into VerificationBanner and verify-email page"
```

---

## Task 3: Wire translations into deliverable components

**Files:**
- Modify: `src/components/deliverable-reject-modal.tsx`
- Modify: `src/components/company-deliverables-tab.tsx`
- Modify: `src/components/influencer-deliverables-tab.tsx`

- [ ] **Step 1: Update `src/components/deliverable-reject-modal.tsx`**

Read the current file. Add `useTranslations("deliverables")` and replace all hardcoded strings:

```typescript
"use client";

import { useState } from "react";
import { useTranslations } from "next-intl";

interface Props {
  deliverableId: string;
  onSuccess: () => void;
  onClose: () => void;
}

export function DeliverableRejectModal({ deliverableId, onSuccess, onClose }: Props) {
  const t = useTranslations("deliverables");
  const REJECT_REASONS = [
    t("reasons.badQuality"),
    t("reasons.doesntMatchBrief"),
    t("reasons.invalidLink"),
    t("reasons.other"),
  ];

  const [reason, setReason] = useState(REJECT_REASONS[0]);
  const [feedback, setFeedback] = useState("");
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleSubmit = async () => {
    if (!feedback.trim()) { setError(t("debriefMissing")); return; }
    setLoading(true);
    setError(null);
    try {
      const res = await fetch(`/api/deliverables/${deliverableId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ action: "reject", feedback: `${reason}: ${feedback}` }),
      });
      if (!res.ok) throw new Error(t("rejectError"));
      onSuccess();
    } catch {
      setError(t("rejectError"));
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm">
      <div className="glass w-full max-w-md rounded-2xl p-6 space-y-4">
        <h2 className="text-lg font-bold text-[#E2E8F0]">{t("rejectTitle")}</h2>

        <div className="space-y-2">
          <p className="text-xs font-semibold uppercase tracking-widest text-[#475569]">{t("reasonLabel")}</p>
          {REJECT_REASONS.map((r) => (
            <label key={r} className="flex items-center gap-2 cursor-pointer">
              <input
                type="radio"
                name="reason"
                value={r}
                checked={reason === r}
                onChange={() => setReason(r)}
                className="accent-violet-500"
              />
              <span className="text-sm text-[#E2E8F0]">{r}</span>
            </label>
          ))}
        </div>

        <div className="space-y-1">
          <p className="text-xs font-semibold uppercase tracking-widest text-[#475569]">
            {t("debriefLabel")} <span className="text-[#F43F5E]">{t("debriefRequired")}</span>
          </p>
          <textarea
            value={feedback}
            onChange={(e) => setFeedback(e.target.value)}
            rows={4}
            placeholder={t("debriefPlaceholder")}
            className="w-full rounded-xl border border-white/[0.08] bg-white/[0.04] px-3 py-2 text-sm text-[#E2E8F0] placeholder-[#64748B] focus:border-violet-500/40 focus:outline-none focus:ring-1 focus:ring-violet-500/20"
          />
          {error && <p className="text-xs text-[#F43F5E]">{error}</p>}
        </div>

        <div className="flex justify-end gap-2">
          <button onClick={onClose} className="rounded-lg border border-white/[0.08] px-4 py-2 text-sm text-[#94A3B8] hover:bg-white/[0.05]">
            {t("../common.cancel", { defaultValue: "Cancel" })}
          </button>
          <button
            onClick={handleSubmit}
            disabled={loading}
            className="rounded-lg bg-[#F43F5E] px-4 py-2 text-sm font-medium text-white hover:bg-[#E11D48] disabled:opacity-50"
          >
            {loading ? t("rejecting") : t("confirmReject")}
          </button>
        </div>
      </div>
    </div>
  );
}
```

**Note on cancel button:** Use `useTranslations("common")` or import separately. Add a second `const tc = useTranslations("common")` and use `tc("cancel")`. Replace the placeholder above with `tc("cancel")`.

- [ ] **Step 2: Update `src/components/company-deliverables-tab.tsx`**

Read the current file. Add `const t = useTranslations("deliverables")` at the top of the component. Replace all French/English hardcoded strings:

Key replacements:
- `"Nouveau livrable"` → `t("newDeliverable")`
- `"Type"` (label) → `t("typeLabel")`
- `"Brief / Description"` → `t("briefLabel")`
- `"Décrivez le contenu attendu..."` → `t("briefPlaceholder")`
- `"Assigner à"` → `t("assignToLabel")`
- `"Tous les influenceurs"` → `t("allInfluencers")`
- `"Créer le livrable"` → `t("createButton")`
- `"Création..."` → `t("creating")`
- `"Erreur lors de la création"` → `t("createError")`
- `"Aucun livrable créé."` → `t("noneCreated")`
- `"Chargement..."` → `t("loading")`
- `"✓ Approuver"` → `t("approve")`
- `"Erreur lors de l'approbation"` → `t("approveError")`
- The type labels array: replace `label:` values with `t(\`types.${value}\`)`
- Status colors map stays the same (CSS only)
- `"Feedback : "` → `t("feedbackLabel")`
- `"Voir le lien de publication →"` → `t("viewPublicationLink")`

For the approve/reject buttons and status badges, use the translation keys.

- [ ] **Step 3: Update `src/components/influencer-deliverables-tab.tsx`**

Read the current file. Add `const t = useTranslations("deliverables")` and replace all hardcoded strings:

Key replacements:
- `"Progression"` → `t("progressLabel")`
- ``{approved}/{total} approuvés`` → `t("approvedCount", { approved, total })`
- `"Aucun livrable assigné pour le moment."` → `t("noneAssigned")`
- `"Chargement..."` → `t("loading")`
- Status badge labels: `"En attente"` → `t("statusPending")`, `"En cours de validation"` → `t("statusSubmitted")`, `"Approuvé ✓"` → `t("statusApproved")`, `"Rejeté"` → `t("statusRejected")`
- `"Feedback de l'entreprise :"` → `t("feedbackLabel")`
- `"Nouveau lien de publication"` → `t("newPublicationLinkLabel")`
- `"Lien de publication"` → `t("publicationLinkLabel")`
- `"https://www.instagram.com/p/..."` → `t("linkPlaceholder")`
- `"Soumettre"` → `t("submitButton")`
- `"Erreur lors de la soumission"` → `t("submitError")`
- `"Voir ma soumission →"` → `t("viewSubmission")`
- Type labels: use `t(\`types.${d.type}\`) || d.type`

- [ ] **Step 4: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Commit**

```bash
git add src/components/deliverable-reject-modal.tsx src/components/company-deliverables-tab.tsx src/components/influencer-deliverables-tab.tsx
git commit -m "feat(i18n): wire translations into deliverable components"
```

---

## Task 4: Wire translations into review and dispute components

**Files:**
- Modify: `src/components/review-modal.tsx`
- Modify: `src/components/review-banner.tsx`
- Modify: `src/components/dispute-modal.tsx`

- [ ] **Step 1: Update `src/components/review-modal.tsx`**

Read the current file. Add `const t = useTranslations("reviews")` and replace all hardcoded strings:

Key replacements:
- `"Laisser un avis"` → `t("leaveReview")`
- `"Votre expérience avec {reviewedName}"` → `t("yourExperience", { name: reviewedName })`
- `"Note"` (label) → `t("noteLabel")`
- `"*"` → `t("required")`
- `"Commentaire"` → `t("commentLabel")`
- `"(optionnel)"` → `t("optional")`
- `"Partagez votre expérience..."` → `t("commentPlaceholder")`
- `"Annuler"` → `tc("cancel")` (use `useTranslations("common")`)
- `"Publier l'avis ✓"` → `t("publishButton")`
- `"Publication..."` → `t("publishing")`
- `"Veuillez sélectionner une note"` → `t("selectRating")`
- Error message → `t("submitError")`

- [ ] **Step 2: Update `src/components/review-banner.tsx`**

Read the current file. Add `const t = useTranslations("reviews")` and replace:
- `"⭐ Campagne terminée !"` → `t("campaignDone")`
- `"Laissez un avis pour {reviewedName}"` → `t("leaveReviewFor", { name: reviewedName })`
- `"Laisser un avis →"` → `t("leaveReviewButton")`

- [ ] **Step 3: Update `src/components/dispute-modal.tsx`**

Read the current file. Add `const t = useTranslations("disputes")` and replace:
- `"Ouvrir un dispute"` (title) → `t("modalTitle")`
- `"Décrivez le problème..."` (desc) → `t("modalDesc")`
- `"Raison du dispute"` → `t("reasonLabel")`
- `"*"` → `t("required")`
- `"Expliquez votre problème..."` → `t("reasonPlaceholder")`
- `"Veuillez décrire votre problème en au moins 10 caractères"` → `t("minLength")`
- `"Annuler"` → use `useTranslations("common")` → `tc("cancel")`
- `"Envoi..."` → `t("submitting")`
- `"Soumettre"` → `t("submit")` (already exists in disputes namespace)
- Error handling text → `t("submitError")` (reuse from disputes or common)

- [ ] **Step 4: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Commit**

```bash
git add src/components/review-modal.tsx src/components/review-banner.tsx src/components/dispute-modal.tsx
git commit -m "feat(i18n): wire translations into review and dispute components"
```

---

## Task 5: Wire translations into campaign detail pages

**Files:**
- Modify: `src/app/[locale]/company/campaigns/[id]/client.tsx`
- Modify: `src/app/[locale]/influencer/campaigns/[id]/client.tsx`
- Modify: `src/components/campaign-chat.tsx`

- [ ] **Step 1: Update `src/app/[locale]/company/campaigns/[id]/client.tsx`**

Read the current file. Add `const t = useTranslations("campaignDetail")` (and `const td = useTranslations("deliverables")`).

Replace tab names:
- `"Infos"` → `t("tabInfos")`
- `"Candidats"` → `t("tabApplicants")`
- `"Livrables"` → `t("tabDeliverables")`
- `"Messages"` → `t("tabMessages")`

Replace InfoTab field labels:
- `"Statut"` → `t("statusLabel")`
- `"Budget"` → `t("budgetLabel")`
- `"Plateformes"` → `t("platformsLabel")`
- `"Créé le"` → `t("createdAtLabel")`
- `"Description"` → `t("descriptionLabel")`

Replace ApplicantsTab:
- `"Aucun candidat pour l'instant."` → `t("noApplicants")`
- Status badge values: use `t(\`participation.${p.status}\`) || p.status`

Replace placeholder tabs content if any:
- Any remaining French placeholder text → use appropriate translation key

- [ ] **Step 2: Update `src/app/[locale]/influencer/campaigns/[id]/client.tsx`**

Read the current file. Add `const t = useTranslations("campaignDetail")`.

Replace tab names:
- `"Détails"` → `t("tabDetails")`
- `"Exécution"` → `t("tabExecution")`
- `"Livrables"` → `t("tabDeliverables")`
- `"Messages"` → `t("tabMessages")`

Replace dispute button text:
- `"Ouvrir un dispute"` → `t("openDispute")`
- `"Dispute soumis. L'équipe vous contactera bientôt."` → `t("disputeSubmitted")`

Replace placeholder content if any:
- `"Messages — à venir (Tâche 10)"` or similar → `t("messagesComingSoon")`

- [ ] **Step 3: Update `src/components/campaign-chat.tsx`**

Read the current file. Add `const t = useTranslations("messaging")` and replace:
- `"Aucune conversation pour cette campagne."` → `t("noConversation")`
- `"Aucun message pour le moment. Démarrez la conversation !"` → `t("noMessages")`
- `"Écrire un message..."` → `t("typePlaceholder")` (already in messaging namespace)
- `"→"` send button → `t("sendButton")`

- [ ] **Step 4: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Commit**

```bash
git add "src/app/[locale]/company/campaigns/[id]/client.tsx" "src/app/[locale]/influencer/campaigns/[id]/client.tsx" src/components/campaign-chat.tsx
git commit -m "feat(i18n): wire translations into campaign detail pages and chat"
```

---

## Task 6: Wire translations into payments page

**Files:**
- Modify: `src/app/[locale]/influencer/payments/page.tsx`
- Modify: `src/app/[locale]/influencer/payments/bank-details-form.tsx`

- [ ] **Step 1: Update `src/app/[locale]/influencer/payments/page.tsx`**

Read the current file. Add `const t = await getTranslations("payments")` and replace all strings:
- Page title `"Paiements"` → `t("title")`
- Subtitle `"Vos revenus..."` → `t("subtitle")`
- `"Total gagné"` → `t("totalEarned")`
- `"En attente"` → `t("pendingAmount")`
- `"Historique"` → `t("historyTitle")`
- `"Aucune transaction."` → `t("noTransactions")`
- Table headers: `"Date"` → `t("colDate")`, `"Montant"` → `t("colAmount")`, `"Statut"` → `t("colStatus")`
- Import: `import { getTranslations } from "next-intl/server"`

- [ ] **Step 2: Update `src/app/[locale]/influencer/payments/bank-details-form.tsx`**

Read the current file. Add `const t = useTranslations("payments")` and replace:
- `"Coordonnées bancaires"` → `t("bankDetailsTitle")`
- `"Ces informations sont utilisées..."` → `t("bankDetailsDesc")`
- `"Nom de la banque"` → `t("bankNameLabel")`
- `"Titulaire du compte"` → `t("accountHolderLabel")`
- `"IBAN / RIB"` → `t("ibanLabel")`
- `"CPA, BNA, BEA..."` → `t("bankNamePlaceholder")`
- `"Prénom Nom"` → `t("accountHolderPlaceholder")`
- `"DZ..."` → `t("ibanPlaceholder")`
- `"Sauvegarder"` → `t("saveButton")`
- `"Sauvegarde..."` → `t("saving")`
- `"Coordonnées sauvegardées ✓"` → `t("savedConfirm")`
- `"Erreur..."` → `t("saveError")`
- `"Chargement..."` → `t("loading")`
- Import: `import { useTranslations } from "next-intl"`

- [ ] **Step 3: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 4: Commit**

```bash
git add "src/app/[locale]/influencer/payments/page.tsx" "src/app/[locale]/influencer/payments/bank-details-form.tsx"
git commit -m "feat(i18n): wire translations into influencer payments page"
```

---

## Task 7: Wire translations into admin pages

**Files:**
- Modify: `src/app/[locale]/admin/influencers/page.tsx`
- Modify: `src/app/[locale]/admin/companies/page.tsx`
- Modify: `src/app/[locale]/admin/campaigns/page.tsx`

- [ ] **Step 1: Update admin/influencers page**

Read the current file. Add `const t = await getTranslations("admin")`.

Replace hardcoded strings:
- Search placeholder `"Rechercher un influenceur..."` (if passed to `AdminSearchInput`) — the placeholder prop on `AdminSearchInput` should use `t("searchInfluencer")`
- Empty state text like `"Aucun résultat pour..."` → use `t("noResultsFor", { query: q })` or `t("noResultsEmpty")`
- Pagination: `"← Précédent"` → `t("previousPage")`, `"Suivant →"` → `t("nextPage")`, `"Page"` → `t("pageLabel")`

- [ ] **Step 2: Update admin/companies page**

Same pattern:
- Search placeholder → `t("searchCompany")`
- Empty state → `t("noResultsFor", { query: q })` or `t("noResultsEmpty")`
- Pagination labels same as above

- [ ] **Step 3: Update admin/campaigns page**

Same pattern:
- Search placeholder → `t("searchCampaign")`
- Status filter label `"Filtrer par statut"` → `t("filterByStatus")`
- All statuses option `"Tous les statuts"` → `t("allStatuses")`
- Empty state → `t("noResultsFor", { query: q })` or `t("noResultsEmpty")`
- Pagination labels same

- [ ] **Step 4: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: Commit**

```bash
git add "src/app/[locale]/admin/influencers/page.tsx" "src/app/[locale]/admin/companies/page.tsx" "src/app/[locale]/admin/campaigns/page.tsx"
git commit -m "feat(i18n): wire translations into admin pages"
```

---

## Task 8: Wire translations into FileUpload component

**Files:**
- Modify: `src/components/file-upload.tsx`

- [ ] **Step 1: Update `src/components/file-upload.tsx`**

Read the current file. The `label` prop already exists so the default value needs to come from translations. Since `FileUpload` is a `"use client"` component, use `useTranslations`:

```typescript
"use client";

import { useRef, useState } from "react";
import { useTranslations } from "next-intl";
import { cn } from "@/components/ui/utils";

interface FileUploadProps {
  value?: string | null;
  onChange: (url: string) => void;
  accept?: string;
  label?: string;
  className?: string;
}

export function FileUpload({ value, onChange, accept = "image/*", label, className }: FileUploadProps) {
  const t = useTranslations("fileUpload");
  const inputRef = useRef<HTMLInputElement>(null);
  const [uploading, setUploading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const displayLabel = label ?? t("label");

  const handleFile = async (file: File) => {
    setError(null);
    setUploading(true);
    try {
      const form = new FormData();
      form.append("file", file);
      const res = await fetch("/api/upload", { method: "POST", body: form });
      const data = await res.json();
      if (!res.ok) throw new Error(data.error || t("error"));
      onChange(data.url);
    } catch (e) {
      setError(e instanceof Error ? e.message : t("error"));
    } finally {
      setUploading(false);
    }
  };

  return (
    <div className={cn("space-y-2", className)}>
      <div
        onClick={() => inputRef.current?.click()}
        className="flex cursor-pointer flex-col items-center justify-center gap-2 rounded-xl border border-dashed border-white/20 bg-white/[0.03] px-4 py-6 transition-colors hover:border-violet-500/40 hover:bg-violet-500/5"
      >
        {value ? (
          // eslint-disable-next-line @next/next/no-img-element
          <img src={value} alt="preview" className="h-24 w-full rounded-lg object-cover" />
        ) : (
          <>
            <svg className="h-8 w-8 text-[#64748B]" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5m-13.5-9L12 3m0 0l4.5 4.5M12 3v13.5" />
            </svg>
            <span className="text-sm text-[#64748B]">{uploading ? t("uploading") : displayLabel}</span>
          </>
        )}
      </div>
      <input
        ref={inputRef}
        type="file"
        accept={accept}
        className="hidden"
        onChange={(e) => {
          const f = e.target.files?.[0];
          if (f) handleFile(f);
        }}
      />
      {error && <p className="text-xs text-[#F43F5E]">{error}</p>}
    </div>
  );
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
git add src/components/file-upload.tsx
git commit -m "feat(i18n): wire translations into FileUpload component"
```

---

## Task 9: Final validation — verify all 3 locales work

- [ ] **Step 1: Verify no untranslated strings remain in new components**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26
grep -rn "Vérifiez\|Renvoyer\|Chargement\|Aucun\|Livrables\|Candidats\|Paiements\|Coordonnées\|Campagne terminée\|Ouvrir un dispute\|Upload en cours\|Choisir une image\|Soumettre\|Approuver\|Rejeter" \
  src/components/verification-banner.tsx \
  src/components/file-upload.tsx \
  src/components/deliverable-reject-modal.tsx \
  src/components/company-deliverables-tab.tsx \
  src/components/influencer-deliverables-tab.tsx \
  src/components/review-modal.tsx \
  src/components/review-banner.tsx \
  src/components/dispute-modal.tsx \
  src/components/campaign-chat.tsx \
  2>/dev/null
```

Expected output: empty (no matches). If matches remain, fix them.

- [ ] **Step 2: Verify all 3 locale files are valid JSON**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26
node -e "require('./messages/en.json'); console.log('EN: OK')"
node -e "require('./messages/fr.json'); console.log('FR: OK')"
node -e "require('./messages/ar.json'); console.log('AR: OK')"
```

Expected: `EN: OK`, `FR: OK`, `AR: OK`.

- [ ] **Step 3: TypeScript full compile**

```bash
cd /Users/macbook/Documents/Dev/UGC26/ugc26 && npx tsc --noEmit 2>&1
```

Expected: no errors.

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "feat(i18n): complete EN/FR/AR translations for entire MVP"
```
