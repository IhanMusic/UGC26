# Adwaa — Redesign Complet UI/UX — Plan d'Implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer la palette violet/indigo par cyan+rose dans les deux modes (dark refait, light mode Glace Électrique), réorganiser les 3 navigations, ajouter icône messages dans le header.

**Architecture:** Phase 1 établit les design tokens (globals.css) — toutes les autres phases dépendent d'elle. Phases 2-3 refont les composants partagés. Phases 4-8 appliquent aux pages dans l'ordre public→auth→dashboards. Chaque phase est autonome une fois Phase 1-3 terminées. Le skill ui-ux-pro-max est utilisé en début de chaque phase.

**Tech Stack:** Next.js 16, React 19, Tailwind CSS v4, CSS Variables, next-themes, next-intl, Lucide React

---

## Fichiers Modifiés

### Phase 1 — Tokens
- Modify: `src/app/globals.css`

### Phase 2 — UI Components
- Modify: `src/components/ui/button.tsx`
- Modify: `src/components/ui/badge.tsx`
- Modify: `src/components/ui/card.tsx`
- Modify: `src/components/ui/input.tsx`
- Modify: `src/components/ui/textarea.tsx`
- Modify: `src/components/ui/checkbox.tsx`
- Modify: `src/components/ui/table.tsx`

### Phase 3 — Navigation & Shell
- Modify: `src/app/[locale]/influencer/_nav.ts`
- Modify: `src/app/[locale]/company/_nav.ts`
- Modify: `src/app/[locale]/admin/_nav.ts`
- Modify: `src/components/nav-link.tsx`
- Modify: `src/components/app-shell.tsx`
- Modify: `src/components/mobile-nav.tsx`
- Modify: `src/components/site-header.tsx`
- Modify: `src/components/cookie-banner.tsx`
- Modify: `src/components/language-switcher.tsx`
- Modify: `src/components/theme-toggle.tsx`
- Modify: `src/components/notification-bell.tsx`
- Modify: `src/components/toast.tsx`
- Modify: `src/components/skeleton.tsx`
- Modify: `src/components/breadcrumbs.tsx`
- Modify: `src/components/empty-state.tsx`

### Phase 4 — Composants Feature
- Modify: `src/components/admin-search-input.tsx`
- Modify: `src/components/admin-status-filter.tsx`
- Modify: `src/components/campaign-chat.tsx`
- Modify: `src/components/contact-influencer-button.tsx`
- Modify: `src/components/deliverable-reject-modal.tsx`
- Modify: `src/components/dispute-modal.tsx`
- Modify: `src/components/file-upload.tsx`
- Modify: `src/components/platform-chips.tsx`
- Modify: `src/components/company-deliverables-tab.tsx`
- Modify: `src/components/influencer-deliverables-tab.tsx`
- Modify: `src/components/review-modal.tsx`

### Phase 5 — Pages Publiques & Auth
- Modify: `src/app/[locale]/layout.tsx`
- Modify: `src/app/[locale]/loading.tsx`
- Modify: `src/app/[locale]/auth/login/client.tsx`
- Modify: `src/app/[locale]/auth/register/influencer/page.tsx`
- Modify: `src/app/[locale]/auth/register/company/page.tsx`
- Modify: `src/app/[locale]/auth/forgot-password/page.tsx`
- Modify: `src/app/[locale]/auth/verify-email/page.tsx`
- Modify: `src/app/[locale]/public/campaigns/page.tsx`
- Modify: `src/app/[locale]/public/campaigns/[id]/page.tsx`
- Modify: `src/app/[locale]/public/influencers/page.tsx`
- Modify: `src/app/[locale]/public/influencers/[id]/page.tsx`
- Modify: `src/app/[locale]/public/companies/[id]/page.tsx`

### Phase 6 — Dashboard Influenceur
- Modify: `src/app/[locale]/influencer/page.tsx`
- Modify: `src/app/[locale]/influencer/profile/client.tsx`
- Modify: `src/app/[locale]/influencer/categories/client.tsx`
- Modify: `src/app/[locale]/influencer/campaigns/page.tsx`
- Modify: `src/app/[locale]/influencer/campaigns/[id]/client.tsx`
- Modify: `src/app/[locale]/influencer/favorites/page.tsx`
- Modify: `src/app/[locale]/influencer/payments/bank-details-form.tsx`

### Phase 7 — Dashboard Entreprise
- Modify: `src/app/[locale]/company/page.tsx`
- Modify: `src/app/[locale]/company/request-campaign/client.tsx`
- Modify: `src/app/[locale]/company/campaigns/page.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/page.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/client.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/applicants/page.tsx`

### Phase 8 — Dashboard Admin & Shared
- Modify: `src/app/[locale]/admin/page.tsx`
- Modify: `src/app/[locale]/admin/requests/page.tsx`
- Modify: `src/app/[locale]/admin/transactions/page.tsx`
- Modify: `src/app/[locale]/admin/influencers/page.tsx`
- Modify: `src/app/[locale]/admin/campaigns/page.tsx`
- Modify: `src/app/[locale]/admin/categories/client.tsx`
- Modify: `src/app/[locale]/admin/companies/page.tsx`
- Modify: `src/app/[locale]/messages/page.tsx`
- Modify: `src/app/[locale]/notifications/page.tsx`

---

## PHASE 1 — Design Tokens

### Task 1: Invoquer ui-ux-pro-max pour validation tokens

- [ ] **Step 1: Obtenir les recommandations design**

```bash
cd ugc26
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "dark mode cyberpunk SaaS UGC platform neon" --design-system -p "Adwaa"
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "light mode glass morphism accessibility contrast" --domain ux
```

- [ ] **Step 2: Vérifier les règles de contraste à respecter**

Le skill confirmera : contraste min 4.5:1, pas de neon sur fond clair, glass cards avec opacité suffisante.

---

### Task 2: Mettre à jour globals.css — Dark Mode (remplacer violet → rose)

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: Remplacer les variables secondary dans `:root`**

Dans `src/app/globals.css`, trouver le bloc `:root { ... }` et remplacer les 3 lignes violet :

```css
/* AVANT */
--secondary:        #8B5CF6;
--secondary-dim:    rgba(139, 92, 246, 0.12);
--secondary-glow:   rgba(139, 92, 246, 0.35);

/* APRÈS */
--secondary:        #FF2D78;
--secondary-dim:    rgba(255, 45, 120, 0.12);
--secondary-glow:   rgba(255, 45, 120, 0.35);
```

- [ ] **Step 2: Mettre à jour bg-mesh pour les orbs rose**

Trouver la classe `.bg-mesh` et remplacer l'orb violet :

```css
.bg-mesh {
  background:
    radial-gradient(ellipse 70% 50% at 10% 20%, rgba(0, 229, 255, 0.07) 0%, transparent 60%),
    radial-gradient(ellipse 50% 60% at 90% 70%, rgba(255, 45, 120, 0.07) 0%, transparent 60%),
    radial-gradient(ellipse 40% 40% at 50% 50%, rgba(0, 255, 136, 0.03) 0%, transparent 60%),
    var(--background);
}
```

- [ ] **Step 3: Renommer .orb-purple → .orb-pink partout dans globals.css**

Trouver `.orb-purple` et renommer en `.orb-pink` avec couleur rose :

```css
.orb-pink {
  background: radial-gradient(circle at center, rgba(255, 45, 120, 0.15) 0%, transparent 70%);
}
```

- [ ] **Step 4: Mettre à jour .tag-neon-purple → .tag-neon-pink**

```css
/* AVANT */
.tag-neon-purple { ... color: #8B5CF6; border-color: rgba(139,92,246,0.3); }

/* APRÈS */
.tag-neon-pink {
  color: var(--secondary);
  border-color: var(--secondary-dim);
  background: var(--secondary-dim);
}
```

- [ ] **Step 5: Mettre à jour .btn-neon pour utiliser var(--secondary) si nécessaire**

Vérifier s'il existe des variantes `.btn-neon-purple` dans globals.css et les convertir en `.btn-neon-pink` avec `var(--secondary)`.

- [ ] **Step 6: Vérifier qu'aucune référence #8B5CF6 ou violet ne reste dans globals.css**

```bash
grep -n "8B5CF6\|8b5cf6\|violet\|indigo" src/app/globals.css
```

Résultat attendu : 0 lignes.

- [ ] **Step 7: Commit**

```bash
git add src/app/globals.css
git commit -m "design: replace violet with rose/magenta in dark mode tokens"
```

---

### Task 3: Mettre à jour globals.css — Light Mode (Glace Électrique)

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: Remplacer le bloc html:not(.dark) complet**

Trouver le bloc `html:not(.dark) { ... }` et le remplacer intégralement :

```css
/* Light mode — Glace Électrique */
html:not(.dark) {
  --background:         #EEF9FF;
  --foreground:         #0F172A;
  --foreground-muted:   #475569;

  --primary:            #0090B8;
  --primary-dim:        rgba(0, 144, 184, 0.08);
  --primary-glow:       rgba(0, 144, 184, 0.20);

  --secondary:          #D91A5E;
  --secondary-dim:      rgba(217, 26, 94, 0.08);
  --secondary-glow:     rgba(217, 26, 94, 0.20);

  --accent:             #047857;
  --accent-dim:         rgba(4, 120, 87, 0.10);

  --gold:               #B45309;
  --gold-dim:           rgba(180, 83, 9, 0.10);

  --danger:             #DC2626;

  --surface:            rgba(255, 255, 255, 0.60);
  --surface-mid:        rgba(255, 255, 255, 0.75);
  --surface-high:       rgba(255, 255, 255, 0.90);
  --surface-hover:      rgba(255, 255, 255, 0.80);

  --border:             rgba(0, 144, 184, 0.15);
  --border-hover:       rgba(0, 144, 184, 0.40);
  --border-bright:      rgba(0, 144, 184, 0.60);

  --shadow-color:       rgba(0, 144, 184, 0.08);
  --shadow-color-hover: rgba(0, 144, 184, 0.16);
  --glow:               rgba(0, 144, 184, 0.12);
}
```

- [ ] **Step 2: Ajouter le fond dégradé glace sur body en light mode**

Après le bloc `html:not(.dark) { ... }`, ajouter :

```css
html:not(.dark) body {
  background: linear-gradient(160deg, #EEF9FF 0%, #F0EDFF 100%);
  background-attachment: fixed;
}
```

- [ ] **Step 3: Ajouter backdrop-blur aux cartes en light mode**

Ajouter une règle pour les `.glass` en light mode :

```css
html:not(.dark) .glass {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(16px) saturate(1.4);
  -webkit-backdrop-filter: blur(16px) saturate(1.4);
  border: 1px solid rgba(0, 144, 184, 0.15);
}

html:not(.dark) .card-cyber {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(0, 144, 184, 0.15);
  box-shadow: 0 4px 24px rgba(0, 144, 184, 0.08);
}

html:not(.dark) .card-cyber:hover {
  border-color: rgba(0, 144, 184, 0.35);
  box-shadow: 0 8px 32px rgba(0, 144, 184, 0.14);
}
```

- [ ] **Step 4: Mettre à jour bg-mesh pour le light mode**

```css
html:not(.dark) .bg-mesh {
  background:
    radial-gradient(ellipse 70% 50% at 10% 20%, rgba(0, 144, 184, 0.06) 0%, transparent 60%),
    radial-gradient(ellipse 50% 60% at 90% 70%, rgba(217, 26, 94, 0.04) 0%, transparent 60%),
    linear-gradient(160deg, #EEF9FF 0%, #F0EDFF 100%);
}
```

- [ ] **Step 5: Vérifier le build TypeScript**

```bash
npx tsc --noEmit
```

Résultat attendu : 0 erreurs.

- [ ] **Step 6: Commit**

```bash
git add src/app/globals.css
git commit -m "design: add Glace Électrique light mode tokens"
```

---

## PHASE 2 — Composants UI de Base

### Task 4: Invoquer ui-ux-pro-max pour les composants

- [ ] **Step 1: Obtenir les recommandations composants**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "button input form card dark light mode" --domain ux
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "interactive elements hover focus accessibility" --stack nextjs
```

---

### Task 5: Refaire button.tsx

**Files:**
- Modify: `src/components/ui/button.tsx`

- [ ] **Step 1: Remplacer le contenu complet du fichier**

```tsx
import * as React from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "./utils";

const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-xl text-sm font-medium transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--primary)] focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--background)] disabled:pointer-events-none disabled:opacity-50 cursor-pointer",
  {
    variants: {
      variant: {
        default:
          "bg-[var(--primary)] text-[var(--background)] shadow-[0_0_16px_var(--primary-glow)] hover:brightness-110 hover:shadow-[0_0_24px_var(--primary-glow)] active:scale-[0.98]",
        secondary:
          "bg-[var(--secondary-dim)] text-[var(--secondary)] border border-[var(--secondary)] hover:bg-[var(--secondary)] hover:text-white active:scale-[0.98]",
        outline:
          "border border-[var(--border)] bg-transparent text-[var(--foreground-muted)] hover:border-[var(--border-hover)] hover:text-[var(--foreground)] hover:bg-[var(--surface)]",
        ghost:
          "text-[var(--foreground-muted)] hover:bg-[var(--surface-mid)] hover:text-[var(--foreground)]",
        destructive:
          "bg-[var(--danger)] text-white shadow-[0_0_12px_rgba(255,59,92,0.3)] hover:brightness-110 active:scale-[0.98]",
        neon:
          "border border-[var(--primary)] text-[var(--primary)] bg-[var(--primary-dim)] hover:bg-[var(--primary)] hover:text-[var(--background)] hover:shadow-[0_0_20px_var(--primary-glow)] active:scale-[0.98]",
      },
      size: {
        default: "h-10 px-5 py-2",
        sm: "h-9 rounded-lg px-3.5 text-xs",
        lg: "h-12 rounded-xl px-8 text-base",
        icon: "h-10 w-10",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

export function Button({
  className,
  variant,
  size,
  asChild = false,
  ...props
}: ButtonProps) {
  const Comp = asChild ? Slot : "button";
  return (
    <Comp
      className={cn(buttonVariants({ variant, size, className }))}
      {...props}
    />
  );
}
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

Résultat attendu : 0 erreurs.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/button.tsx
git commit -m "design: button — CSS variables, rose secondary, neon variant"
```

---

### Task 6: Refaire badge.tsx

**Files:**
- Modify: `src/components/ui/badge.tsx`

- [ ] **Step 1: Remplacer le contenu complet**

```tsx
import * as React from "react";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "./utils";

const badgeVariants = cva(
  "inline-flex items-center gap-1.5 rounded-full border px-3 py-1 text-xs font-medium transition-colors",
  {
    variants: {
      variant: {
        default:
          "border-[var(--border)] bg-[var(--surface-mid)] text-[var(--foreground-muted)]",
        primary:
          "border-[var(--primary)] bg-[var(--primary-dim)] text-[var(--primary)]",
        secondary:
          "border-[var(--secondary)] bg-[var(--secondary-dim)] text-[var(--secondary)]",
        success:
          "border-[var(--accent)] bg-[var(--accent-dim)] text-[var(--accent)]",
        warning:
          "border-[var(--gold)] bg-[var(--gold-dim)] text-[var(--gold)]",
        danger:
          "border-[var(--danger)] bg-[rgba(255,59,92,0.1)] text-[var(--danger)]",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
);

export interface BadgeProps
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof badgeVariants> {}

export function Badge({ className, variant, ...props }: BadgeProps) {
  return (
    <div className={cn(badgeVariants({ variant }), className)} {...props} />
  );
}
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/badge.tsx
git commit -m "design: badge — CSS variables, rose/cyan/gold variants"
```

---

### Task 7: Refaire card.tsx, input.tsx, textarea.tsx, checkbox.tsx, table.tsx

**Files:**
- Modify: `src/components/ui/card.tsx`
- Modify: `src/components/ui/input.tsx`
- Modify: `src/components/ui/textarea.tsx`
- Modify: `src/components/ui/checkbox.tsx`
- Modify: `src/components/ui/table.tsx`

- [ ] **Step 1: Lire les fichiers actuels**

```bash
cat src/components/ui/card.tsx
cat src/components/ui/input.tsx
cat src/components/ui/textarea.tsx
cat src/components/ui/checkbox.tsx
cat src/components/ui/table.tsx
```

- [ ] **Step 2: Consulter ui-ux-pro-max pour les formulaires**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "form input label error focus accessibility" --domain ux
```

- [ ] **Step 3: Remplacer card.tsx**

Supprimer toute référence `violet-*`, `slate-*`, `white/` hardcodées. Utiliser :
- Fond carte : `bg-[var(--surface)] border border-[var(--border)]`
- Hover : `hover:border-[var(--border-hover)] hover:shadow-[0_0_20px_var(--shadow-color-hover)]`
- Header/title : `text-[var(--foreground)]`
- Description : `text-[var(--foreground-muted)]`

- [ ] **Step 4: Remplacer input.tsx**

```tsx
// className de base pour <input>
"flex h-10 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-sm text-[var(--foreground)] placeholder:text-[var(--foreground-muted)] transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-[var(--primary)] focus:border-[var(--border-hover)] focus-visible:ring-[var(--primary)] disabled:cursor-not-allowed disabled:opacity-50"
```

- [ ] **Step 5: Remplacer textarea.tsx**

Même pattern que input.tsx mais avec `min-h-[80px] resize-y`.

- [ ] **Step 6: Remplacer checkbox.tsx**

```tsx
// Checkbox checked state:
"data-[state=checked]:bg-[var(--primary)] data-[state=checked]:border-[var(--primary)] data-[state=checked]:text-[var(--background)]"
// Border non-checked:
"border-[var(--border)] bg-[var(--surface)]"
```

- [ ] **Step 7: Remplacer table.tsx**

Supprimer `violet-*`, remplacer par :
- Header : `text-[var(--foreground-muted)] border-b border-[var(--border)]`
- Row hover : `hover:bg-[var(--surface-hover)]`
- Border : `border-[var(--border)]`

- [ ] **Step 8: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 9: Commit**

```bash
git add src/components/ui/card.tsx src/components/ui/input.tsx src/components/ui/textarea.tsx src/components/ui/checkbox.tsx src/components/ui/table.tsx
git commit -m "design: ui primitives — CSS variables, dark/light via tokens"
```

---

## PHASE 3 — Navigation & Shell

### Task 8: Invoquer ui-ux-pro-max pour la navigation

- [ ] **Step 1: Obtenir les recommandations navigation**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "sidebar navigation dashboard SaaS mobile responsive" --domain ux
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "layout responsive sidebar" --stack nextjs
```

---

### Task 9: Reorganiser les 3 fichiers _nav.ts

**Files:**
- Modify: `src/app/[locale]/influencer/_nav.ts`
- Modify: `src/app/[locale]/company/_nav.ts`
- Modify: `src/app/[locale]/admin/_nav.ts`

- [ ] **Step 1: Remplacer influencer/_nav.ts**

Le type retourné doit inclure un champ `section` optionnel pour les séparateurs :

```ts
import { getTranslations } from "next-intl/server";

export type NavItem =
  | { type: "link"; href: string; label: string }
  | { type: "section"; label: string };

export async function getInfluencerNav(): Promise<NavItem[]> {
  const t = await getTranslations("nav");
  return [
    { type: "link",    href: "/influencer",         label: t("dashboard") },
    { type: "section", label: t("navSection.mySpace") },
    { type: "link",    href: "/influencer/profile",  label: t("profile") },
    { type: "link",    href: "/influencer/categories", label: t("myCategories") },
    { type: "link",    href: "/influencer/payments", label: t("paymentInfo") },
    { type: "section", label: t("navSection.activity") },
    { type: "link",    href: "/influencer/campaigns", label: t("myCampaigns") },
    { type: "link",    href: "/creator/pitches",     label: t("myPitches") },
    { type: "link",    href: "/influencer/favorites", label: t("favorites") },
    { type: "section", label: t("navSection.explore") },
    { type: "link",    href: "/public/campaigns",    label: t("browseCampaigns") },
    { type: "link",    href: "/public/influencers",  label: t("browseInfluencers") },
  ];
}
```

- [ ] **Step 2: Remplacer company/_nav.ts**

```ts
import { getTranslations } from "next-intl/server";
import type { NavItem } from "../influencer/_nav";

export async function getCompanyNav(): Promise<NavItem[]> {
  const t = await getTranslations("nav");
  return [
    { type: "link",    href: "/company",                    label: t("dashboard") },
    { type: "section", label: t("navSection.myCompany") },
    { type: "link",    href: "/company/profile",            label: t("profile") },
    { type: "section", label: t("navSection.campaigns") },
    { type: "link",    href: "/company/request-campaign",   label: t("requestCampaign") },
    { type: "link",    href: "/company/campaigns",          label: t("myCampaigns") },
    { type: "link",    href: "/company/completions",        label: t("completions") },
    { type: "section", label: t("navSection.creatorsProjects") },
    { type: "link",    href: "/company/projets",            label: t("creatorProjects") },
    { type: "link",    href: "/company/sponsorships",       label: t("sponsorships") },
    { type: "link",    href: "/public/influencers",         label: t("browseInfluencers") },
    { type: "section", label: t("navSection.finance") },
    { type: "link",    href: "/company/expenses",           label: t("expenses") },
  ];
}
```

- [ ] **Step 3: Remplacer admin/_nav.ts**

```ts
import { getTranslations } from "next-intl/server";
import type { NavItem } from "../influencer/_nav";

export async function getAdminNav(): Promise<NavItem[]> {
  const t = await getTranslations("nav");
  return [
    { type: "link",    href: "/admin",                 label: t("dashboard") },
    { type: "section", label: t("navSection.users") },
    { type: "link",    href: "/admin/influencers",     label: t("influencer") },
    { type: "link",    href: "/admin/companies",       label: t("company") },
    { type: "link",    href: "/admin/categories",      label: t("categories") },
    { type: "section", label: t("navSection.validation") },
    { type: "link",    href: "/admin/requests",        label: t("requestedCampaigns") },
    { type: "link",    href: "/admin/applications",    label: t("preValidation") },
    { type: "link",    href: "/admin/pitches",         label: t("pitchQueue") },
    { type: "section", label: t("navSection.operations") },
    { type: "link",    href: "/admin/campaigns",       label: t("campaigns") },
    { type: "link",    href: "/admin/transactions",    label: t("transactions") },
    { type: "link",    href: "/admin/disputes",        label: t("disputes") },
    { type: "section", label: t("navSection.system") },
    { type: "link",    href: "/admin/settings",        label: t("settings") },
  ];
}
```

- [ ] **Step 4: Ajouter les clés manquantes dans messages/fr.json, en.json, ar.json**

Dans chaque fichier `messages/*.json`, ajouter sous la clé `"nav"` :

```json
"navSection": {
  "mySpace": "Mon Espace",
  "activity": "Activité",
  "explore": "Explorer",
  "myCompany": "Mon Entreprise",
  "campaigns": "Campagnes",
  "creatorsProjects": "Créateurs & Projets",
  "finance": "Finance",
  "users": "Utilisateurs",
  "validation": "Validation",
  "operations": "Opérations",
  "system": "Système"
},
"myPitches": "Mes Projets (Pitchs)",
"pitchQueue": "File Pitchs",
"creatorProjects": "Projets Créateurs",
"sponsorships": "Mes Sponsorisations"
```

Pour `en.json` et `ar.json`, traduire ces clés en anglais et arabe respectivement.

- [ ] **Step 5: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/app/[locale]/influencer/_nav.ts src/app/[locale]/company/_nav.ts src/app/[locale]/admin/_nav.ts messages/fr.json messages/en.json messages/ar.json
git commit -m "design: nav — grouped sections, remove emojis, add NavItem type"
```

---

### Task 10: Refaire nav-link.tsx

**Files:**
- Modify: `src/components/nav-link.tsx`

- [ ] **Step 1: Mettre à jour pour supporter NavItem type (section + link)**

```tsx
"use client";

import { usePathname, Link } from "@/i18n/navigation";
import { cn } from "@/components/ui/utils";
import type { ReactNode } from "react";
import type { NavItem } from "@/app/[locale]/influencer/_nav";

interface NavLinkProps {
  item: NavItem;
  icon?: ReactNode;
  onClick?: () => void;
}

export function NavLink({ item, icon, onClick }: NavLinkProps) {
  const pathname = usePathname();

  if (item.type === "section") {
    return (
      <div className="px-3 pt-5 pb-1 text-[10px] font-bold uppercase tracking-widest text-[var(--foreground-muted)] select-none">
        {item.label}
      </div>
    );
  }

  const isActive = pathname === item.href || pathname.startsWith(item.href + "/");

  return (
    <Link
      href={item.href}
      onClick={onClick}
      className={cn(
        "group flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition-all duration-200",
        isActive
          ? "bg-[var(--primary-dim)] text-[var(--primary)]"
          : "text-[var(--foreground-muted)] hover:bg-[var(--surface-mid)] hover:text-[var(--foreground)]"
      )}
    >
      {icon ? (
        <span className={cn(
          "flex h-4 w-4 items-center justify-center transition-colors",
          isActive ? "text-[var(--primary)]" : "text-[var(--foreground-muted)] group-hover:text-[var(--foreground)]"
        )}>
          {icon}
        </span>
      ) : (
        <div className={cn(
          "h-1.5 w-1.5 rounded-full flex-shrink-0 transition-all duration-200",
          isActive
            ? "bg-[var(--primary)] shadow-[0_0_6px_var(--primary-glow)]"
            : "bg-[var(--border-hover)] group-hover:bg-[var(--primary)]"
        )} />
      )}
      <span>{item.label}</span>
      {isActive && (
        <span className="ml-auto h-1.5 w-1.5 rounded-full bg-[var(--primary)] shadow-[0_0_6px_var(--primary-glow)]" />
      )}
    </Link>
  );
}
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/components/nav-link.tsx
git commit -m "design: nav-link — CSS variables, section header support"
```

---

### Task 11: Refaire app-shell.tsx

**Files:**
- Modify: `src/components/app-shell.tsx`

- [ ] **Step 1: Mettre à jour les imports (ajouter MessageSquare de lucide-react)**

```tsx
import { MessageSquare } from "lucide-react";
import type { NavItem } from "@/app/[locale]/influencer/_nav";
```

- [ ] **Step 2: Mettre à jour la signature du composant**

```tsx
export async function AppShell({
  title,
  nav,
  children,
}: {
  title: string;
  nav: NavItem[];
  children: React.ReactNode;
})
```

- [ ] **Step 3: Remplacer la sidebar complète**

```tsx
<aside className="hidden w-72 flex-col border-r border-[var(--border)] bg-[var(--surface-high)] backdrop-blur-2xl md:flex"
       style={{ padding: "20px" }}>
  {/* Logo */}
  <div className="flex items-center justify-between">
    <Link href="/" className="flex items-center gap-2.5 group">
      <div
        className="flex h-8 w-8 items-center justify-center rounded-lg font-display font-black text-sm text-black transition-all duration-300 group-hover:scale-110"
        style={{ background: "var(--primary)", boxShadow: "0 0 12px var(--primary-glow)" }}
      >
        A
      </div>
      <span className="font-display font-bold text-lg tracking-tight gradient-text-cyber">
        ADWAA
      </span>
    </Link>
    {session?.user ? (
      <Badge variant="primary" className="text-[10px]">{session.user.role}</Badge>
    ) : null}
  </div>

  {/* Navigation */}
  <nav className="mt-6 flex flex-col" aria-label="Dashboard navigation">
    {nav.map((item, i) => (
      <NavLink key={item.type === "link" ? item.href : `section-${i}`} item={item} />
    ))}
  </nav>

  {/* User info */}
  <div className="mt-auto pt-6">
    {session?.user ? (
      <div className="space-y-3 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-4">
        <div className="flex items-center gap-3">
          <div
            className="flex h-9 w-9 items-center justify-center rounded-full text-xs font-bold text-black"
            style={{ background: "var(--primary)", boxShadow: "0 0 10px var(--primary-glow)" }}
          >
            {session.user.email?.charAt(0).toUpperCase()}
          </div>
          <div className="min-w-0 flex-1">
            <div className="truncate text-xs font-medium text-[var(--foreground)]">
              {session.user.email}
            </div>
            <div className="text-[10px] text-[var(--foreground-muted)]">
              {t("common.signedInAs")}
            </div>
          </div>
        </div>
        <SignOutButton variant="outline" className="w-full" size="sm">
          {t("common.signOut")}
        </SignOutButton>
      </div>
    ) : null}
  </div>
</aside>
```

- [ ] **Step 4: Remplacer le header — ajouter l'icône Messages**

```tsx
<header className="border-b border-[var(--border)] bg-[var(--surface-high)] px-4 py-4 backdrop-blur-2xl">
  <div className="mx-auto flex max-w-6xl items-center justify-between">
    <div>
      <div className="text-[10px] font-medium uppercase tracking-widest text-[var(--primary)]">
        {t("common.dashboard")}
      </div>
      <h1 className="text-xl font-bold tracking-tight font-display text-[var(--foreground)]">
        {title}
      </h1>
    </div>
    <div className="flex items-center gap-2">
      {/* Messages icon */}
      <Link
        href="/messages"
        className="flex h-9 w-9 items-center justify-center rounded-xl border border-[var(--border)] bg-[var(--surface)] text-[var(--foreground-muted)] transition-all duration-200 hover:border-[var(--border-hover)] hover:text-[var(--primary)]"
        aria-label={t("nav.messages")}
      >
        <MessageSquare className="h-4 w-4" />
      </Link>
      <NotificationBell />
      <ThemeToggle />
      <LanguageSwitcher />
      {session?.user ? (
        <Badge variant="primary" className="hidden sm:inline-flex md:hidden text-[10px]">
          {session.user.role}
        </Badge>
      ) : null}
      <MobileNav nav={nav} role={session?.user?.role} />
    </div>
  </div>
</header>
```

- [ ] **Step 5: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/components/app-shell.tsx
git commit -m "design: app-shell — CSS variables, messages icon, grouped nav"
```

---

### Task 12: Refaire mobile-nav.tsx

**Files:**
- Modify: `src/components/mobile-nav.tsx`

- [ ] **Step 1: Mettre à jour pour NavItem type et CSS variables**

```tsx
"use client";

import { useState } from "react";
import { Link } from "@/i18n/navigation";
import { NavLink } from "@/components/nav-link";
import type { NavItem } from "@/app/[locale]/influencer/_nav";

export function MobileNav({
  nav,
  role,
}: {
  nav: NavItem[];
  role?: string;
}) {
  const [open, setOpen] = useState(false);

  return (
    <div className="md:hidden">
      <button
        onClick={() => setOpen(!open)}
        className="flex h-10 w-10 items-center justify-center rounded-xl border border-[var(--border)] bg-[var(--surface)] text-[var(--foreground-muted)] transition-all duration-200 hover:border-[var(--border-hover)] hover:text-[var(--primary)]"
        aria-label="Toggle navigation menu"
        aria-expanded={open}
      >
        {open ? (
          <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        ) : (
          <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        )}
      </button>

      {open && (
        <>
          <div
            className="fixed inset-0 z-40 bg-[var(--background)]/60 backdrop-blur-sm"
            onClick={() => setOpen(false)}
          />
          <nav
            className="fixed inset-y-0 left-0 z-50 w-72 border-r border-[var(--border)] bg-[var(--surface-high)] backdrop-blur-2xl p-5 shadow-2xl overflow-y-auto"
            role="navigation"
            aria-label="Mobile navigation"
          >
            <div className="flex items-center justify-between mb-6">
              <Link href="/" className="flex items-center gap-2.5" onClick={() => setOpen(false)}>
                <div
                  className="flex h-8 w-8 items-center justify-center rounded-lg font-display font-black text-sm text-black"
                  style={{ background: "var(--primary)", boxShadow: "0 0 12px var(--primary-glow)" }}
                >
                  A
                </div>
                <span className="font-display font-bold text-lg gradient-text-cyber">ADWAA</span>
              </Link>
              <button
                onClick={() => setOpen(false)}
                className="flex h-8 w-8 items-center justify-center rounded-lg text-[var(--foreground-muted)] hover:bg-[var(--surface-mid)] transition-colors"
                aria-label="Close menu"
              >
                <svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                  <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>

            {role && (
              <div className="mb-4 inline-flex items-center rounded-full border border-[var(--primary)] bg-[var(--primary-dim)] px-3 py-1 text-xs font-medium text-[var(--primary)]">
                {role}
              </div>
            )}

            <div className="flex flex-col">
              {nav.map((item, i) => (
                <NavLink
                  key={item.type === "link" ? item.href : `section-${i}`}
                  item={item}
                  onClick={() => item.type === "link" && setOpen(false)}
                />
              ))}
            </div>
          </nav>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/components/mobile-nav.tsx
git commit -m "design: mobile-nav — CSS variables, NavItem type, rose/cyan palette"
```

---

### Task 13: Refaire site-header.tsx, cookie-banner.tsx, language-switcher.tsx, theme-toggle.tsx

**Files:**
- Modify: `src/components/site-header.tsx`
- Modify: `src/components/cookie-banner.tsx`
- Modify: `src/components/language-switcher.tsx`
- Modify: `src/components/theme-toggle.tsx`

- [ ] **Step 1: Mettre à jour site-header.tsx — supprimer `!py-2 !px-4` inline, utiliser Button neon**

Dans `site-header.tsx`, remplacer les classes hardcodées `.btn-solid-cyan`, `.btn-neon` par le composant Button :

```tsx
// Remplacer btn-solid-cyan par Button variant="default"
<Button size="sm" asChild>
  <Link href={roleHome(session.user.role ?? "")}>
    {t("common.dashboard")}
  </Link>
</Button>

// Remplacer btn-neon par Button variant="neon"
<Button variant="neon" size="sm" asChild>
  <Link href="/auth/register/influencer">
    {t("nav.register")}
  </Link>
</Button>
```

- [ ] **Step 2: Mettre à jour cookie-banner.tsx**

```tsx
"use client";

import { useState, useEffect } from "react";
import { useTranslations } from "next-intl";
import { Button } from "@/components/ui/button";

export function CookieBanner() {
  const [visible, setVisible] = useState(false);
  const t = useTranslations("cookies");

  useEffect(() => {
    if (!localStorage.getItem("cookie-consent")) {
      setVisible(true);
    }
  }, []);

  const accept = () => {
    localStorage.setItem("cookie-consent", "true");
    setVisible(false);
  };

  if (!visible) return null;

  return (
    <div className="fixed bottom-0 left-0 right-0 z-50 border-t border-[var(--border)] bg-[var(--surface-high)] backdrop-blur-2xl px-4 py-3">
      <div className="mx-auto flex max-w-5xl items-center justify-between gap-4">
        <p className="text-sm text-[var(--foreground-muted)]">{t("banner")}</p>
        <Button onClick={accept} size="sm" className="shrink-0">
          {t("accept")}
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Mettre à jour language-switcher.tsx**

```tsx
"use client";

import { usePathname } from "@/i18n/navigation";
import { locales, type AppLocale } from "@/i18n/routing";
import { useLocale } from "next-intl";

const flags: Record<AppLocale, string> = { fr: "FR", en: "EN", ar: "AR" };

export function LanguageSwitcher() {
  const locale = useLocale() as AppLocale;
  const pathname = usePathname();

  const switchLocale = (l: AppLocale) => {
    document.cookie = `NEXT_LOCALE=${l}; path=/; max-age=31536000; SameSite=Lax`;
    window.location.href = `/${l}${pathname}`;
  };

  return (
    <div className="flex items-center gap-1 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-1">
      {locales.map((l) => (
        <button
          key={l}
          onClick={() => switchLocale(l)}
          className={`rounded-lg px-2.5 py-1 text-xs font-medium transition-all duration-200 cursor-pointer ${
            l === locale
              ? "bg-[var(--primary)] text-[var(--background)] shadow-[0_0_8px_var(--primary-glow)]"
              : "text-[var(--foreground-muted)] hover:bg-[var(--surface-mid)] hover:text-[var(--foreground)]"
          }`}
          aria-label={`Switch to ${l}`}
          aria-current={l === locale ? "true" : undefined}
        >
          {flags[l]}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Mettre à jour theme-toggle.tsx — remplacer violet par CSS vars**

Lire `src/components/theme-toggle.tsx` et remplacer toute référence `violet-*` par `text-[var(--primary)]` / `hover:bg-[var(--surface-mid)]`.

- [ ] **Step 5: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/components/site-header.tsx src/components/cookie-banner.tsx src/components/language-switcher.tsx src/components/theme-toggle.tsx
git commit -m "design: shell components — CSS variables, cookie/lang/theme/header"
```

---

### Task 14: Refaire notification-bell.tsx, toast.tsx, breadcrumbs.tsx, empty-state.tsx, skeleton.tsx

**Files:**
- Modify: `src/components/notification-bell.tsx`
- Modify: `src/components/toast.tsx`
- Modify: `src/components/breadcrumbs.tsx`
- Modify: `src/components/empty-state.tsx`
- Modify: `src/components/skeleton.tsx`

- [ ] **Step 1: Lire chaque fichier**

```bash
cat src/components/notification-bell.tsx
cat src/components/toast.tsx
cat src/components/breadcrumbs.tsx
cat src/components/empty-state.tsx
cat src/components/skeleton.tsx
```

- [ ] **Step 2: Dans chaque fichier, remplacer le pattern violet → CSS vars**

Pattern de remplacement à appliquer :
- `violet-600` / `violet-500` → `[var(--primary)]`
- `violet-500/15` / `violet-50` → `[var(--primary-dim)]`
- `violet-400` / `violet-300` → `text-[var(--primary)]`
- `indigo-*` → `[var(--primary)]`
- `slate-900` / `slate-800` → `[var(--foreground)]`
- `slate-600` / `slate-500` → `[var(--foreground-muted)]`
- `slate-200` / `slate-100` → `[var(--border)]`
- `bg-white` / `bg-white/80` → `bg-[var(--surface-high)]`
- `dark:bg-slate-900` → supprimer (géré par CSS vars)

- [ ] **Step 3: Pour skeleton.tsx spécifiquement**

```tsx
// Classe de base skeleton
"animate-pulse rounded-md bg-[var(--surface-mid)]"
```

- [ ] **Step 4: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Vérifier aucun violet dans les composants partagés**

```bash
grep -n "violet\|indigo" src/components/notification-bell.tsx src/components/toast.tsx src/components/breadcrumbs.tsx src/components/empty-state.tsx src/components/skeleton.tsx
```

Résultat attendu : 0 lignes.

- [ ] **Step 6: Commit**

```bash
git add src/components/notification-bell.tsx src/components/toast.tsx src/components/breadcrumbs.tsx src/components/empty-state.tsx src/components/skeleton.tsx
git commit -m "design: shared components — CSS variables throughout"
```

---

## PHASE 4 — Composants Feature

### Task 15: Invoquer ui-ux-pro-max + refaire les composants feature

**Files:**
- Modify: `src/components/admin-search-input.tsx`
- Modify: `src/components/admin-status-filter.tsx`
- Modify: `src/components/campaign-chat.tsx`
- Modify: `src/components/contact-influencer-button.tsx`
- Modify: `src/components/deliverable-reject-modal.tsx`
- Modify: `src/components/dispute-modal.tsx`
- Modify: `src/components/file-upload.tsx`
- Modify: `src/components/platform-chips.tsx`
- Modify: `src/components/company-deliverables-tab.tsx`
- Modify: `src/components/influencer-deliverables-tab.tsx`
- Modify: `src/components/review-modal.tsx`

- [ ] **Step 1: Invoquer ui-ux-pro-max pour les formulaires et modales**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "modal dialog form upload chat interaction" --domain ux
```

- [ ] **Step 2: Lire chaque fichier pour identifier les références violet**

```bash
grep -n "violet\|indigo" src/components/admin-search-input.tsx src/components/admin-status-filter.tsx src/components/file-upload.tsx src/components/platform-chips.tsx
```

- [ ] **Step 3: Appliquer le pattern de remplacement sur chaque fichier**

Même pattern de remplacement qu'en Task 14 :
- `violet-*` → `[var(--primary)]` ou `[var(--primary-dim)]`
- `indigo-*` → `[var(--primary)]`
- `slate-900` / `slate-800` → `[var(--foreground)]`
- `slate-600` / `slate-500` → `[var(--foreground-muted)]`
- `bg-white` → `bg-[var(--surface-high)]`

Pour `file-upload.tsx` spécifiquement, la zone de drop doit utiliser :
```tsx
"border-2 border-dashed border-[var(--border)] bg-[var(--surface)] hover:border-[var(--primary)] hover:bg-[var(--primary-dim)] transition-all duration-200"
```

Pour `platform-chips.tsx`, chaque plateforme peut garder sa couleur propre (YouTube = rouge, Instagram = rose, TikTok = noir) mais le fallback doit utiliser `var(--primary-dim)`.

Pour `campaign-chat.tsx`, les bulles de message :
- Message envoyé : `bg-[var(--primary-dim)] border border-[var(--border)] text-[var(--foreground)]`
- Message reçu : `bg-[var(--surface-mid)] text-[var(--foreground)]`

Pour les modales (`deliverable-reject-modal.tsx`, `dispute-modal.tsx`, `review-modal.tsx`) :
- Fond modal : `bg-[var(--surface-high)] backdrop-blur-xl border border-[var(--border)]`
- Overlay : `bg-[var(--background)]/70 backdrop-blur-sm`
- Boutons d'action : utiliser `Button` avec les variants appropriés

- [ ] **Step 4: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Vérifier aucun violet dans les composants feature**

```bash
grep -rn "violet\|indigo" src/components/admin-search-input.tsx src/components/admin-status-filter.tsx src/components/campaign-chat.tsx src/components/contact-influencer-button.tsx src/components/deliverable-reject-modal.tsx src/components/dispute-modal.tsx src/components/file-upload.tsx src/components/platform-chips.tsx src/components/company-deliverables-tab.tsx src/components/influencer-deliverables-tab.tsx src/components/review-modal.tsx
```

Résultat attendu : 0 lignes.

- [ ] **Step 6: Commit**

```bash
git add src/components/admin-search-input.tsx src/components/admin-status-filter.tsx src/components/campaign-chat.tsx src/components/contact-influencer-button.tsx src/components/deliverable-reject-modal.tsx src/components/dispute-modal.tsx src/components/file-upload.tsx src/components/platform-chips.tsx src/components/company-deliverables-tab.tsx src/components/influencer-deliverables-tab.tsx src/components/review-modal.tsx
git commit -m "design: feature components — CSS variables throughout"
```

---

## PHASE 5 — Pages Publiques & Auth

### Task 16: Invoquer ui-ux-pro-max pour les pages publiques

- [ ] **Step 1: Obtenir recommandations pour les pages publiques**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "landing page SaaS UGC hero CTA dark mode" --design-system -p "Adwaa"
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "auth login register form accessibility" --domain ux
```

---

### Task 17: Mettre à jour layout.tsx et loading.tsx

**Files:**
- Modify: `src/app/[locale]/layout.tsx`
- Modify: `src/app/[locale]/loading.tsx`

- [ ] **Step 1: Lire layout.tsx**

```bash
cat src/app/[locale]/layout.tsx
```

- [ ] **Step 2: Dans layout.tsx, remplacer les meta couleurs violet**

Chercher dans les metadata toute référence à violet/indigo dans `themeColor` ou OG. Remplacer par `#00E5FF`.

- [ ] **Step 3: Mettre à jour loading.tsx**

Supprimer le spinner violet et le remplacer par un spinner cyan :

```tsx
// spinner
<div className="h-10 w-10 rounded-full border-2 border-[var(--border)] border-t-[var(--primary)] animate-spin" />
```

- [ ] **Step 4: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/layout.tsx src/app/[locale]/loading.tsx
git commit -m "design: layout/loading — cyan theme, CSS variables"
```

---

### Task 18: Refaire les pages Auth

**Files:**
- Modify: `src/app/[locale]/auth/login/client.tsx`
- Modify: `src/app/[locale]/auth/register/influencer/page.tsx`
- Modify: `src/app/[locale]/auth/register/company/page.tsx`
- Modify: `src/app/[locale]/auth/forgot-password/page.tsx`
- Modify: `src/app/[locale]/auth/verify-email/page.tsx`

- [ ] **Step 1: Lire tous les fichiers auth**

```bash
cat src/app/[locale]/auth/login/client.tsx
cat src/app/[locale]/auth/register/influencer/page.tsx
```

- [ ] **Step 2: Appliquer le pattern de remplacement sur chaque fichier auth**

Pattern à appliquer (même que Phase 2-4) :
- `violet-600` → `[var(--primary)]`
- `violet-500/15` → `[var(--primary-dim)]`
- `slate-*` → variables CSS appropriées
- `bg-white` → `bg-[var(--surface-high)]`
- `focus:ring-violet-*` → `focus:ring-[var(--primary)]`

Pour les formulaires auth, ajouter `role="alert"` sur les messages d'erreur si absent.

Pour les cartes de formulaire :
```tsx
"w-full max-w-md rounded-2xl border border-[var(--border)] bg-[var(--surface-high)] p-8 backdrop-blur-xl shadow-[0_0_40px_var(--shadow-color)]"
```

- [ ] **Step 3: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Vérifier aucun violet dans les pages auth**

```bash
grep -rn "violet\|indigo" src/app/[locale]/auth/
```

Résultat attendu : 0 lignes.

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/auth/
git commit -m "design: auth pages — CSS variables, rose/cyan palette"
```

---

### Task 19: Refaire les pages public/browse

**Files:**
- Modify: `src/app/[locale]/public/campaigns/page.tsx`
- Modify: `src/app/[locale]/public/campaigns/[id]/page.tsx`
- Modify: `src/app/[locale]/public/influencers/page.tsx`
- Modify: `src/app/[locale]/public/influencers/[id]/page.tsx`
- Modify: `src/app/[locale]/public/companies/[id]/page.tsx`

- [ ] **Step 1: Invoquer ui-ux-pro-max pour les pages de browse**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "browse directory card grid filter search" --domain ux
```

- [ ] **Step 2: Appliquer le pattern de remplacement sur chaque fichier**

Même pattern violet → CSS vars. Pour les cartes de campagne/influenceur :
- Fond : `bg-[var(--surface)] border border-[var(--border)] hover:border-[var(--border-hover)]`
- Badge statut : utiliser `<Badge>` avec le variant approprié (success/warning/danger)
- Liens de filtre actifs : `text-[var(--primary)] border-b-2 border-[var(--primary)]`

- [ ] **Step 3: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add src/app/[locale]/public/
git commit -m "design: public browse pages — CSS variables"
```

---

## PHASE 6 — Dashboard Influenceur

### Task 20: Invoquer ui-ux-pro-max + refaire pages influenceur

**Files:**
- Modify: `src/app/[locale]/influencer/page.tsx`
- Modify: `src/app/[locale]/influencer/profile/client.tsx`
- Modify: `src/app/[locale]/influencer/categories/client.tsx`
- Modify: `src/app/[locale]/influencer/campaigns/page.tsx`
- Modify: `src/app/[locale]/influencer/campaigns/[id]/client.tsx`
- Modify: `src/app/[locale]/influencer/favorites/page.tsx`
- Modify: `src/app/[locale]/influencer/payments/bank-details-form.tsx`

- [ ] **Step 1: Invoquer ui-ux-pro-max pour le dashboard influenceur**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "influencer dashboard profile campaign payment" --design-system -p "Adwaa"
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "form validation profile edit bank details" --domain ux
```

- [ ] **Step 2: Appliquer le pattern de remplacement sur chaque fichier**

Même pattern. Pour le dashboard `page.tsx` (stats cards) :
```tsx
// Stat card
"rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6 hover:border-[var(--border-hover)] transition-all duration-200"
// Valeur stat
"text-3xl font-bold font-display text-[var(--foreground)]"
// Label stat
"text-sm text-[var(--foreground-muted)]"
// Icône stat
"text-[var(--primary)]"  // ou secondary/accent/gold selon le type
```

Pour `bank-details-form.tsx` (formulaire paiement) :
- Inputs : utiliser le composant `<Input>` refait en Phase 2
- Labels : `text-sm font-medium text-[var(--foreground)]`
- Erreurs : `text-xs text-[var(--danger)]` + `role="alert"`

- [ ] **Step 3: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Vérifier aucun violet**

```bash
grep -rn "violet\|indigo" src/app/[locale]/influencer/
```

Résultat attendu : 0 lignes.

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/influencer/
git commit -m "design: influencer dashboard — CSS variables throughout"
```

---

## PHASE 7 — Dashboard Entreprise

### Task 21: Invoquer ui-ux-pro-max + refaire pages entreprise

**Files:**
- Modify: `src/app/[locale]/company/page.tsx`
- Modify: `src/app/[locale]/company/request-campaign/client.tsx`
- Modify: `src/app/[locale]/company/campaigns/page.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/page.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/client.tsx`
- Modify: `src/app/[locale]/company/campaigns/[id]/applicants/page.tsx`

- [ ] **Step 1: Invoquer ui-ux-pro-max pour le dashboard entreprise**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "company brand dashboard campaign management applicants" --design-system -p "Adwaa"
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "table list filter status management" --domain ux
```

- [ ] **Step 2: Appliquer le pattern de remplacement sur chaque fichier**

Pour la page applicants (table avec statuts) :
- Status badges : utiliser `<Badge>` avec variants (success=validé, warning=en attente, danger=refusé, secondary=nouveau)
- Boutons d'action : utiliser `<Button>` avec variants appropriés
- Table header : `text-xs font-bold uppercase tracking-widest text-[var(--foreground-muted)]`

Pour `request-campaign/client.tsx` (formulaire multi-étapes) :
- Progress bar : `bg-[var(--primary)]` sur fond `bg-[var(--surface-mid)]`
- Étape active : `text-[var(--primary)] border-[var(--primary)]`
- Étape passée : `text-[var(--accent)] border-[var(--accent)]`

- [ ] **Step 3: Vérifier TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Vérifier aucun violet**

```bash
grep -rn "violet\|indigo" src/app/[locale]/company/
```

Résultat attendu : 0 lignes.

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/company/
git commit -m "design: company dashboard — CSS variables throughout"
```

---

## PHASE 8 — Dashboard Admin & Pages Partagées

### Task 22: Invoquer ui-ux-pro-max + refaire pages admin

**Files:**
- Modify: `src/app/[locale]/admin/page.tsx`
- Modify: `src/app/[locale]/admin/requests/page.tsx`
- Modify: `src/app/[locale]/admin/transactions/page.tsx`
- Modify: `src/app/[locale]/admin/influencers/page.tsx`
- Modify: `src/app/[locale]/admin/campaigns/page.tsx`
- Modify: `src/app/[locale]/admin/categories/client.tsx`
- Modify: `src/app/[locale]/admin/companies/page.tsx`
- Modify: `src/app/[locale]/messages/page.tsx`
- Modify: `src/app/[locale]/notifications/page.tsx`

- [ ] **Step 1: Invoquer ui-ux-pro-max pour le dashboard admin**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "admin dashboard management validation transactions disputes" --design-system -p "Adwaa"
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "data table pagination search filter admin" --domain ux
```

- [ ] **Step 2: Appliquer le pattern de remplacement sur les pages admin**

Pour les pages de listing (influenceurs, entreprises, campagnes) :
- Search input : utiliser `<Input>` refait avec icône Lucide `Search`
- Filtres de statut : utiliser `<Badge>` comme boutons de filtre actif/inactif
- Tableaux : utiliser le composant `<Table>` refait

Pour `transactions/page.tsx` :
- Montants positifs : `text-[var(--accent)]`
- Montants négatifs : `text-[var(--danger)]`
- Montants neutres : `text-[var(--foreground)]`

Pour `categories/client.tsx` (CRUD catégories) :
- Formulaire inline : mêmes styles input/button
- Tags de catégories : `<Badge variant="primary">`

- [ ] **Step 3: Refaire messages/page.tsx**

La page messages doit maintenant être accessible depuis l'icône header. Vérifier que le layout est correct (pas de doublon nav) et appliquer les CSS vars.

- [ ] **Step 4: Refaire notifications/page.tsx**

Même approche — CSS vars, plus de violet.

- [ ] **Step 5: Vérifier TypeScript final**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Vérification finale — aucun violet dans tout le projet**

```bash
grep -rn "violet\|indigo\|#8B5CF6\|8b5cf6" src/ --include="*.tsx" --include="*.ts" --include="*.css"
```

Résultat attendu : 0 lignes.

- [ ] **Step 7: Build final**

```bash
npm run build
```

Résultat attendu : build réussi sans erreurs.

- [ ] **Step 8: Commit final**

```bash
git add src/app/[locale]/admin/ src/app/[locale]/messages/ src/app/[locale]/notifications/
git commit -m "design: admin + shared pages — CSS variables, redesign complete"
```

---

## Vérification Responsive

### Task 23: Validation responsive et pre-delivery checklist

- [ ] **Step 1: Invoquer ui-ux-pro-max pre-delivery**

```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "responsive mobile tablet desktop layout" --domain ux
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "accessibility contrast focus keyboard" --domain ux
```

- [ ] **Step 2: Démarrer le serveur de dev**

```bash
npm run dev
```

- [ ] **Step 3: Checklist visuelle dark mode**

Vérifier sur http://localhost:3000 :
- [ ] Fond `#01010C`, texte `#D8EEFF` visible
- [ ] Cyan `#00E5FF` sur boutons primaires avec glow
- [ ] Rose `#FF2D78` sur badges secondary / boutons secondary
- [ ] Aucun violet visible nulle part
- [ ] Menus avec sections groupées et labels
- [ ] Icône messages dans le header (à côté de la cloche)
- [ ] Cookie banner visible et stylé correctement

- [ ] **Step 4: Basculer en light mode et vérifier**

- [ ] Fond dégradé glace visible (`#EEF9FF → #F0EDFF`)
- [ ] Cartes avec effet glass (backdrop-blur)
- [ ] Cyan foncé `#0090B8` lisible sur fond clair
- [ ] Rose foncé `#D91A5E` lisible sur fond clair
- [ ] Contraste texte ≥ 4.5:1

- [ ] **Step 5: Vérifier responsive**

Redimensionner le navigateur :
- [ ] 375px : hamburger visible, sidebar masquée, contenu 1 colonne
- [ ] 768px : sidebar apparaît ou drawer selon le layout
- [ ] 1024px : sidebar fixe 288px, contenu max-w-6xl
- [ ] 1440px : centré, pas de scroll horizontal

- [ ] **Step 6: Commit final**

```bash
git add -A
git commit -m "design: responsive validation, full redesign complete"
```
